node_modules/
dist/
build/
.claude/
knowledge/
ui/dist-v3/
.DS_Store
/analysis/
monitor.log
dumps/

# Copyrighted disk/cart images — NEVER commit (Spec 771 incident: motm.g64 had
# leaked to the public repo root). Real game images stay local-only; only the
# synthetic test data under the samples/ whitelist below is tracked.
*.g64
*.d64
*.crt
samples/commercial/
samples/*
!samples/test-manifest.json
!samples/test-manifest.schema.json
!samples/README.md
!samples/vice-testprogs/
!samples/vice-testprogs/**
docs/windows-powershell-setup.md
resources/c64ref-rom-knowledge.json
resources/roms/*.bin
resources/fingerprints/trex/
resources/fingerprints/local/
traces/
/runtime/
!samples/screenshots/
samples/screenshots/*
!samples/screenshots/proof/
!samples/screenshots/proof/**
!samples/golden-master/
!samples/golden-master/**
!samples/fastloader-tests/
!samples/fastloader-tests/**
!samples/fixtures/
!samples/fixtures/**
tmp/
session/
snapshots/

# reSID WASM (Spec 703). Source vendored in third_party/resid/ (GPL).
# The prebuilt module IS committed under src/runtime/headless/sid/wasm/ so a
# plain checkout/npm install needs no emscripten — only the maintainer rebuilds
# it with `npm run build:resid-wasm`. Ignore only emcc scratch + the dist copy.
third_party/resid/build/
dist/runtime/headless/sid/wasm/

# scratch / probe outputs (never commit)
.tmp/

# EF-port candidate scan (research scratch, untracked)
ef-port-candidates/
