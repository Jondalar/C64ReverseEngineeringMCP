# VICE1541

New side-by-side 1541 implementation for Spec 611.

This directory is intentionally empty of runtime behavior in phase 611.0.
The current implementation remains in `drive/`, `iec/`, and `via/` as
LEGACY1541 and is selected by default until VICE1541 passes the runtime
proof gates.
