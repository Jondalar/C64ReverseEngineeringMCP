# LEGACY1541

This directory is the current TypeScript 1541 implementation selected by
default in Spec 611.

It is frozen for behavior changes while `src/runtime/headless/vice1541/`
is built side-by-side. Keep these files in place until VICE1541 becomes
the default and the runtime proof gate stays green.
