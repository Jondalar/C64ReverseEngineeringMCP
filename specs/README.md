# Specs

**Everything here is open work.** Finished, retired and decided-against specs, each with
the decision that closed it: [`_archive/README.md`](_archive/README.md).
Rules that govern how work is done are not specs: [`../DOCTRINE.md`](../DOCTRINE.md).

Numbers are **shared with TRX64** (`../../TRX64/docs/`) — one range, one registry.
**Next free: 869.** (807 = TRX64's binary checkpoint ring, closed and archived there; 808 = TRX64's rewind
transport, 809 = TRX64's marks and branches — both tracked on the TRX64 board. 808 has a C64RE-side
ribbon on the existing scrub UI; 810 is C64RE's and is listed below.)

| # | Spec | Status | What is left | Touched |
|---|---|---|---|---|
| 716 | [C64RE distribution: npm + install docs](716-installation-versioning-distribution.md) | READY | The C64RE counterpart 801 deferred, now scoped. First step is a gate, not a publish: work out what a published tarball must contain and whether the entry point runs from `node_modules` without a repo around it. TRX64's answers (one version, tag = `--version`, tag-driven CI, the package manager as the install doc) are the template. | 2026-08-11 |
| 720 | [Disassembly Output Quality](720-disasm-output-quality.md) | READY | DRAFT. Heuristic auto-labels + box headers in the phase-1 disassembler. Core C64RE meaning. | 2026-05-23 |
| ~~750~~ | [Disk + Cartridge Cartography](_archive/750-disk-cartridge-cartography-visualization.md) | **CLOSED 2026-08-12** | All seven slices built; every addressing kind in its own model implemented. A table is a record (identity · layout · column roles · per-column semantics), rows derive from it, the claim `payload ↔ (table,row)` persists, both surfaces draw the index and its footprint, and `writes` is a warning rather than a fact. The find-a-table path was REBUILT after two real cartridges showed byte-shape scanning cannot do it — the anchor is the instruction the loader compiles to. `e2e:750-lut` 85/85 plus `e2e:750-real`, which runs against a real image and skips loudly without one. | 2026-08-12 |
| 774 | [Capability Cut → `trx64-static`](_archive/774-capability-cut-static-migration.md) | **CLOSED 2026-09-19** | Step 1 built (the runtime's shared decoder), step 2 dropped, step 3 WON'T-DO: TRX64 is a runtime — for `trx64cli`, C64RE and UE2 — and static analysis stays C64RE's, in TS. | 2026-09-19 |
| 775 | [Decoupled Agent/Flow Layer (BMAD)](775-decoupled-agent-flow-layer-bmad.md) | READY | Private in-repo module; docks onto 773. Gate: pin the V6 schema first. | 2026-07-03 |
| 800 | [Runtime invisible to the RE agent](800-runtime-invisible-setup-guided.md) | READY | §A–§D built and now **gated** (`npm run check:runtime-invisible`): 64 agent-facing surfaces scanned for the backend brand, plus the epoch and the per-OS recipe. It was red when written — a trace-domain description had gained "served by the TRX64 daemon" the same day, which is the argument for the gate. Open: the guided setup probe, now including **goal 4 — the recipe fetches the runtime itself**. One TypeScript step for all three OSes instead of a scoop manifest and a winget PR; never silent, checksum-verified, pinned to the epoch the handshake expects. Also answers the Windows install gap 801 left behind. | 2026-08-11 |
| 804 | [Symbols are joined in C64RE](_archive/804-symbols-joined-in-c64re.md) | **BUILT 2026-09-19 — merged, both repos** | Moved from TRX64 and rewritten on the owner's decision: TRX64 is a runtime and holds **no symbols at all**; C64RE owns meaning, so names are joined here. TRX64 lost its label layer (`label`/`note`/`sl`/`ll`/`sym`/`inspect`/`xref`, `resolvePc`, every write into the project — already broken by the 822.2 cut-over) and every `monitor/exec` reply now carries address SPANS plus the banking state. C64RE: one resolver over three layers (user > build > derived, origin on every name), residency decided by matching a payload's code bytes against live memory (no match → no name), per trace row as well; TRX64 owns the command set — C64RE substitutes names and decorates spans generically, with no verb list. Gates: TRX64 gate GREEN (396 daemon tests, 7/7 games), `smoke-804-resolver` 42/0, `smoke-804-monitor` 25/0 (`a 1100 jmp set_border` reached the runtime as `a 1100 jmp $1010`). Wire: `resolvePc` removed → an epoch bump (`trx64-runtime/2`) is due at release. Open: a "name this address" door (the `label` verb had no successor). | 2026-09-19 |
| 844 | [The questions are a schema](_archive/844-the-questions-are-a-schema.md) | **BUILT** | The owner, deep in Ultima VI with a project session: the model disassembles semantically and does not keep the relationships, doctrine has to be re-requested every session, and he is the one putting things in relation from outside — "die Fragen sind aber in allen Multi-Disk-Projekten dieselben". That last sentence is the diagnosis: same questions every time means they are not questions but a SCHEMA with empty slots. The model has not forgotten — `save_finding` is optional and nothing ever required a relationship to be named. Doctrine as prose works exactly as long as someone reads it aloud, and that someone is him. His own rule from 773/775 bounds the fix — "C64RE ≠ 2. LLM-Runtime" — so an orchestrator that drives the model is out, and determinism can only live where it cannot be talked out of: a tool that REFUSES on an unmet precondition (the 834/835/839 pattern), and a data model where an empty required slot is a QUERY RESULT, so "mapped" is computable rather than asserted. 775 is adjacent and does not solve it: it makes the agent layer portable, and it already calls that layer "inert data" — portable inert data is still inert. The slot list is now IN (§4): his ten slots — context, medium, boot chain, data geometry, runtime count, runtime linkage, engine presence, engine architecture, modules, save model — plus four the corpus of seven finished projects (Accolade, Brubaker, Fire King, Lykia, Wasteland, Neuromancer, Ultima VI) failed on repeatedly and never wrote down: free RAM (four projects claimed it from reading, all four were corrected by running), coverage (Neuromancer says EXHAUSTIVE at ~15 %), the per-project evidence standard, and refutations that name the instrument that was wrong. REFUSE-or-REPORT is decided per slot, which is what having the list makes possible. The corpus law across all of it: **a scan proves presence, never absence** — Ultima VI produced four false negatives that way, each costing a rebuild, and our own analyzer hallucinated a depacker and invented a JSR at $1BC9 from an incomplete opcode-length table. D4+D5 BUILT: the owner read the existing read-before-runtime gate and put his finger through it — "was ist denn die Hypothese ohne die verweigert wird? doch einfach Text" (a $XXXX regex plus 20 characters, so pure invention passed). A citation must now RESOLVE against routine nodes / findings / entities / graph nodes, dormant while a project holds no analysis so it cannot brick day one; and an accrual ratchet refuses gated runtime/build doors after 4 calls that left no durable record, released by writing ANY record including a refutation — the Ultima VI runtime→build loop he had to break by hand. `npm run e2e:844-teeth`, ten cases. D1+D2 BUILT too: `src/slots/` holds the fifteen slots as data, `slotReport()` makes "what is still unmapped" a query (filled / hypothesis / empty / n·a + computed coverage), and eight doors refuse on the slot that guards them — register_payload and extract_disk_custom_lut on S4/S2, the two link tools on S3, runtime_candidate_patch and runtime_overlay_run on S11, render_docs on S7/S9/S10, save_finding on the S12 vocabulary. `project_slots` + `slot_record` are the two DEFAULT-surface tools. Two corrections the build forced on §4: S4 may not be derived from payload entities (register_payload would have fed its own gate) and gating starts at one knowledge record (a slot list on an empty directory refuses the first call anyone makes). `npm run e2e:844-slots` (24 cases) and `npm run e2e:844-teeth` (10). **Amended 2026-09-17 — S15, and the fourteen became fifteen.** Issue #24, from another owner's session after a two-hour debugging session it described as having "totally fucked over" its owner: on *Crazy News* side 1 the BAM's free list and actual occupancy are disjoint IN BOTH DIRECTIONS — all 30 blocks it calls free are in use by an asset, all 78 genuinely spare blocks are marked allocated, and the per-track counts disagree with their own bitmaps. A patch step asked the BAM for a spare block, got T1/S1, overwrote another asset's chain head, and the symptom surfaced much later as executing data on the in-game screen. The instructive part is that the session's model was RIGHT: its report answers S4 in full — link chains, addressed by track/sector, "the game never reads the BAM" — and then it stepped outside the question set to write. All fourteen slots describe what the GAME does; none asked where a TOOL may put bytes, so at the moment of writing there was no question of ours to answer and the DOS convention was the only one left. S15 "Writable space" is S11 one level out and keeps S11's shape: `method:chains` (every chain walked and subtracted) settles it, `method:bam` is a hypothesis and stays one. REPORT, not REFUSE — no door here allocates medium space (`runtime_media_persist` writes back the game's own allocation), so gating one would punish the wrong action. Fixed in the same breath: `disk_sector_allocation`'s matrix row said "Map BAM/sector allocation" and the tool never read the BAM — ownership comes from the extraction manifest — so the description offered as a free-list the one thing that is not one. The new gate case also exposed a defect far older than the slot: `slotReport` derived `media` from the artifact list it had already narrowed to MEASURABLE_KINDS (prg/raw/extract), so a .d64 could never reach it — on a disk-only project S2 never filled from its own registered images and S10 was permanently n/a, meaning **the save model went unasked on every game that has one**. Every fixture in the suite registered a prg, so nothing caught it; S15's case is the first that registers a disk. Coverage and medium are now two lists. Open: the coverage threshold in S12 and the ratchet threshold, both build decisions for the first project that runs them. | 2026-09-12 |
| 845 | [The model layer](_archive/845-the-model-layer.md) | **BUILT** | The owner on why the Ultima VI HTML model matters and the HTML does not: "es SOLL NICHT NUR im Kontext sein, sondern persistent, damit es nach 3 mal compact oder /new auch weiter den Zusammenhang sieht → Graph" — and on the goal behind it, "ich WILL mich wegrationalisieren in der Analysephase". Measured against that project's own graph, two of the three expected gaps were the wrong way round: 94 % of its 24 780 EDGES already carry evidence, while 0 of its 11 161 NODES do, and CONTAINS is intra-routine (routine→label) so it cannot carry hierarchy. The real gap is a coarse level ABOVE the graph — the session's model is 177 nodes where the graph is 11 161, and it fits in a re-entering session's head precisely because of that. Decisions: a model layer with an explicit level; boundaries asserted but membership computed by address containment (844 D3 one level up, so a dozen assertions index eleven thousand nodes); every model node carries its own citation; container edges rolled up from the fine ones, inheriting them as evidence; orphans as a query result, a finer completeness measure than S12's byte coverage; and one re-entry read that returns the model, what is open, and the refutations — six in Ultima VI, each stopping a rebuild down a path already known wrong. D7 settles the 844 seam: two doors, not one — only three or four of the fifteen slots are container-shaped, so a merged door would push ten others through a container API; instead `slot_record` asserts the boundary too, so the model layer fills as a side effect of answering 844's questions. Open: whether `level` is a closed set of three, decidable at build time against a cart project. BUILT: `src/model/` + `model_assert` / `model_read` / `model_remove` on the DEFAULT surface, `npm run e2e:845-model` (22 cases). The build forced one correction — a boundary is identified by its NAME, not its start address: the first cut derived the id from the range's first byte and a component at $2000-$20FF inside a container at $2000-$3FFF silently overwrote its own parent, so model nodes use the subsystem id form and carry their range in attrs. `settleBy` is named as NOT built (OpenQuestionRecord has no tag carrier) and moves to the critic spec with gaps and nextRead. Critic/verdict and the render are explicitly out. | 2026-09-12 |
| 846 | [The critic](_archive/846-the-critic.md) | **BUILT** | The third of the three things that stop being present when the owner leaves the analysis phase — 844 replaced his FRAME (the fifteen slots), 845 his MEMORY (the model layer and its re-entry read), and this one his COUNTER-PRESSURE: he was the one who did not believe "create.prg writes nothing". 844's ratchet counts runtime calls that left no record, and a counter is not an adversary — it is satisfiable with save_finding("looked at $4100"), which is obvious while he watches and indistinguishable from work when he does not. The central claim: most of what he caught is MECHANICALLY checkable, because the corpus law cuts both ways. A negative claim is hard to establish by scanning and easy to REFUTE by scanning — one contradicting edge is enough — and all four of Ultima VI's rebuild-costing false negatives are refutable against that project's own graph ($F3 has USES_ZP edges, $4800-$53FF has edges landing in it, "only $3E83 writes" has siblings). So D1 is a negative-claim detector and D2 six more graph-only checks, including "refutation without a casualty" (a refutation whose target still stands as active) and "finding without evidence", which closes 844's ratchet leak. D3 answers the calibration risk — a critic that finds nothing is worthless, one that finds everything blocks — by making severity a property of the CHECK, declared once in an arguable table, never a per-instance judgement. D4 is the verdict: readyToDesign, computed, allowed to say no, naming what would flip it; it absorbs 844's checkPhaseComplete. D5 holds 773 decision #1 by narrowing the resolution: between REFUSING and DRIVING lies ASKING, so what the graph can decide C64RE decides and what needs prose against prose it FORMULATES as a question for the harness — it never holds a model client. D6 gives settleBy the carrier 845 named as missing. BUILT: `src/critic/` + `project_critique` / `critic_checks` on the DEFAULT surface, `npm run e2e:846-critic` (22 cases). D1 is fired at Ultima VI's three address-shaped false negatives and the graph refutes all three while leaving the one true statement in the same set alone — precision is asserted, not hoped for. D6 turned out smaller than 845 recorded: `saveOpenQuestion` always persisted `attrs.tags` and only the read path dropped them, so 845's "no carrier" note was wrong. Two corrections the test forced: `empty-boundary` and `orphan-ratio` used to vanish from the "checks run" list when a project had no boundaries, which makes silence indistinguishable from breakage; and the `only $X writes` refutation printed the claimed address instead of naming the other doer. FIRST REAL RUN (Ultima VI, read-only, graph byte-identical after): two bugs the synthetic test could not find — USES_ZP edges point at PLATFORM ids that are not rows in the project's nodes table, so the check joined past all 4970 zero-page edges and missed "$F3 is read by nothing" in the very project whose rebuild it cost; and the object-position negative ("create.prg writes nothing") did not parse. Both fixed, all four historical false negatives now refuted against the real graph with the edge named, the true statement left alone. Live findings: 6 blocking, all refutation-without-casualty (none of U6's six refutations records what it invalidated — that lives in an HTML file, not the graph), 25 unreachable drivecode entries, 0 findings without evidence. Also forced a rewrite of 844's S12: the denominator summed 27 MB of generated .asm and unioned address ranges across 315 separate PRGs, capping the ratio structurally and reporting 0.1 %; per-owner over loadable artifacts it is 30.2 % of 1641238 bytes. Open: the prompt shape for the handed-over half and the orphan ratio. | 2026-09-12 |
| 847 | [Documents declare themselves](_archive/847-documents-declare-themselves.md) | **BUILT** | Spec 740.1 shipped the wiki skeleton in May; both long-running projects still show `docs/index.md` at 880 bytes with 7 of 7 categories reading "(no curated entries yet)", and 740.2 — the tool that would have written them — is BACKLOG and does not exist. But the synthesis WAS written: Ultima VI's docs/ holds EF_PORT_SPEC.md (59 KB), RAM_BUDGET.md, nine model documents and a 310 KB HTML, none of which anything indexes, lints or can cite. The wiki was not routed around because it was a wiki — it asked for DUPLICATE work with no payoff to whoever did it, and that trade loses every time. Frontmatter has the inverse economy. D1 is therefore narrow and demands no new documents: every synthesis document that EXISTS declares what it covers, and what it covers resolves in the graph. D3 refuses the owner's own suggestion of a JSON store beside the graph — 822.2 removed exactly that and DOCTRINE.md records why three copies drifted — so a document becomes a node instead, which makes 845's boundaries able to cite one and 846's `amends:` resolve instead of pointing nowhere (Ultima VI has six refutations naming documents, none of which resolves today). D4 gives generated docs their provenance so staleness is computable — FINDINGS.md renders 19 of 41 findings, three days old, silently — and fixes a search-index defect where the stale render supplies a second older copy of the same fact. D6 makes the wiki index DERIVED, so it cannot sit empty for four months, and answers 740.2 rather than building it. D7 answers the owner's hook question: a hook CAN enforce this and must not be the primary layer, because a hook lives in the harness and the harness is not the product — Codex, CI and a handed-over project get nothing. The logic goes in a `c64re doc lint` CLI, the hook is three lines calling it, and it refuses by handing back the template rather than by saying no. Both open points then settled by looking instead of asking: the field set mirrors what the documents ALREADY write in prose — U6's A_overlay_model.md opens with its coverage ($0200-$437E), its sources, and its evidence standard ("the binary wins; every 'all stores to X' claim is an exhaustive opcode scan, not a grep"), which is 844's S13 at document level where it is sharper — minus `claims`, dropped because not one document in any project names a finding id. And documents live in any NESTED docs/ directory: Wasteland_EF's 108 files "outside docs/" are all in cart_EF/docs/engine, editor/docs and siblings. No project document carries frontmatter today, so every one is undeclared on day one — which is why the lint calls its list a BACKLOG, not a failure. BUILT: `src/docs/` + `doc_register`/`doc_lint`/`doc_template`/`wiki_index` on the DEFAULT surface, `c64re doc lint|check|index` as the CLI a hook calls, `dangling-citation` and `stale-render` as the critic's 8th and 9th checks, and render_docs stamping its own provenance. `npm run e2e:847-docs` (27 cases). One design point the test forced: the template's placeholders deliberately do NOT parse — making them parseable would let a pasted-but-unedited template silently declare a wrong address range — so the placeholder is refused by name instead. | 2026-09-12 |
| 848 | [The contract, and what a name is worth](_archive/848-the-contract-and-what-a-name-is-worth.md) | **BUILT** | Out of the first unattended run. An agent given four Neuromancer disks and no mention of 844-847 produced 273 entities, 209 findings and a complete static model of the fastloader in 27 minutes — with 0 routine nodes, 0 annotation files, 99.2 % coverage and 0.0 % NAMED. It understood a great deal and named none of it. Ultima VI, months of attended work, reads 30.2 % coverage and 78.9 % named: coverage alone ranks the unattended run above it. And one thing jammed in a way no single spec could predict — the agent hit 844's S4 gate, complied, filled five slots with slot_record, and every one of those became an `ungrounded` finding that parked Spec 752's step recommender on "run analyze_prg" for the rest of the session, while rule 8 ("source exists but has no annotations") was true the whole time and never reached. The more correctly it followed one spec, the harder it jammed another. D1 is the contract: what the human expects, written once before leaving, asking about DELIVERY and never about facts (at kickoff nobody knows whether the game saves — that is the work), allowed to demand FEWER slots than the default fifteen, and the home for the three thresholds that were global defaults picked blind. D2 makes named-ness its own measure. D3 makes a blocker able to VETO but never to decide — 752 now shuts the runtime branch, which is what it always meant, and names the cure instead of repeating the trigger, because a rule whose recommendation cannot repair its own trigger is an infinite loop by construction. D4 grounds slot_record. D5 hands the contract over first at onboarding. Three corrections the real data forced on D2: a payload named after its directory entry cannot count (one painted a project 100 % named); the graph's layers must be merged by id first (U6: 1853 routines, 799 with extent, 978 with a name, zero with both); and it is counted per NODE, because an annotation gives a name and a start address, never an extent. VALIDATED on a second unattended run — same disks, same folder, the SAME PROMPT word for word, only the machinery and the contract differing: annotation files 0 -> 5, named routines 0 -> 40, NAMED 0.0 % -> 39.3 %, boundaries 2 -> 9, declared documents 0 -> 1, slots 7 -> 9. It missed the contract by 0.7 points and closed its own plan with "danach ist auch die 40-%-Namensquote erledigt" — planning against a number that was never in its prompt — and left S11 a hypothesis with the words "NOT established: nothing has been run". A third session then answered six questions about the game from the project alone, under the rule that it may not re-analyse, and found the uncertainty as well as the claims (nothing executed, S11 unestablished, two empty boundaries, one retracted claim). It also found the day's worst defect, which was mine: EVERY read tool across 845-848 paired its report with structuredContent and the client shows only the structured half, so `model_read` returned counters instead of the model — agent_onboard was unaffected precisely because it returns text and nothing else, which is why the handover worked while these did not. Also fixed from that run: a document demand now resolves a role through the model to a range (it was comparing "loader" against $3E00-$42F9), and createId is non-deterministic BY DESIGN so the version-decision question deduplicated nothing and filed itself on every sync — 11 of 13 open questions were that, burying the two real ones. `npm run e2e:848-contract`. Open: the saturation guard, and the frozen workflow-state.json. | 2026-09-12 |
| 849 | [Harness rules](_archive/849-harness-rules.md) | **BUILT** | Measured, not proposed: `.claude/rules/*.md` with a `paths:` glob was probed on this machine — a session that read only `src/code.asm` received the nested CLAUDE.md block and the rule globbed to `**/*.asm`, and did NOT receive the one globbed to `**/*.json`. Glob targeting is real and deterministic, and it fills the gap 844-848 named repeatedly and never closed: the onboarding handover speaks at session start, the door gates refuse at delivery, and nothing spoke at the moment the work happens — "you just disassembled this, it is not understood until its routines carry names", "you just corrected yourself, record it as a refutation". A rule fires because a glob matched, not because the model judged the task correctly, which is the same defect 848 D3 was written for. What it is NOT: enforcement. The same day's run received the contract at onboarding, followed its form completely, and still recorded copy protection that does not exist. Three layers, each limited to its own failure kind: rules speak at the moment of work (advisory), gates refuse at delivery (binding), the critic refutes from the graph (form and contradiction only). BUILT: six rules, one per moment the three unattended runs passed through silently (listing read but not annotated, names without boundaries, `probable_code` read as code, G64 checksum failures called protection, prose without frontmatter, a fact written into the contract). Shipped from `assets/project-rules/`, copied in by `project_init` and re-synced by `agent_onboard` — init alone would freeze a project's rules at the day it was created. A hand-edited rule is never overwritten: the ledger holds the hash of what was SHIPPED, not of what is on disk. A rule announces the door that will refuse, and `smoke:project-rules` checks every announced refusal against the real slot table — an announced refusal that does not happen teaches the session that rules are decoration. S14 gets no rule: a retraction touches no file, so no glob can fire there. MEASURED and the mechanism was wrong: in a 102-turn unattended run not one rule fired — a `paths:` glob fires on a native READ, and an RE session reads through the MCP tools (four native file accesses in the whole run, all writes). Isolating probe: Read fires, Write does not. Rebuilt so the moment belongs to the TOOL — each rule names the tools that are its moment and they append it to their own result, once per session, re-armed by agent_onboard; a project's hand-edited wording wins over the shipped text. The rule TEXTS did show through on the one run that read them by accident (S13 quoted the heuristics rule, S14 rejected a 0.95-confidence LUT hit, S2 declined to call gcr_errors protection) — evidence for the texts, none for the delivery. Two defects the run surfaced: the extract-first doctrine collides with a "no own scripts" prompt and left four slots empty, and disasm_prg could not find `<outdir>/<stem>_annotations.json` — the obvious name, since propose_annotations leaves its draft right there. Re-measured on run 5: 5 of 6 rules delivered through their own tool, 9/10 slots filled against run 3's 7 and run 4's 6, 2 blockers left (only S11, which correctly wants a run). The sixth rule never fired because its only trigger was propose_annotations and the run wrote annotations by hand — disasm_prg now carries it too, and a tool may carry several rules. Not separated from curiosity: both runs read the rule files themselves at their start. Run 7 then put the CONTRACT on the write path (every recording tool carries what is still owed, as a delta; the summary tools always answer) after measuring that writes are the densest channel — 14-24 per run, the last landing in the final 5 % of the session. Effect: model_assert 0 → 12, project_critique 0 → 2 with both findings fixed, orphans 62 % → 9.1 %, boundaries 2 → 12, at 2.2× the tokens. It still stopped with three blockers standing after reading them — which is the line this layer cannot cross: a stop is not a tool call, so enforcing it belongs to a loop outside the session. | 2026-09-13 |
| 810 | [Scenario goals and acceptance](_archive/810-scenario-goals-and-acceptance.md) | PARTLY BUILT | What is checked and who says yes. The load-bearing idea: **acceptance converts a verbal goal into a byte-exact one** — a human says yes once, that state is frozen, and from then on the same criterion is a 794 diff with nobody present. The exclusion mask belongs to the CRITERION, not the run, or no two tests are comparable. **Re-scoped 2026-08-17 after reading what exists:** the runner engine and the branch object are NOT open — 796's candidate is the branch (baseline anchor + accumulating patch-set + bound replay, shipped 2026-07-16), and 812 built the sandbox substrate and measured that driving it over a socket stays deterministic. Open: joining mark→candidate→`sandbox/run`, the acceptance STORE (types only, nothing persists — the part of which nothing exists), byte-exact evaluation against the ACCEPTED baseline rather than 796's no-patch one (the spatial mask it needed shipped with 813 on 2026-08-18; what is still missing is the STORE to compare against), the `# targets:` indexer with its lint, and a door — there is no MCP tool for 810 at all. | 2026-08-17 |
| 811 | [Hardware bus stream as a second evidence source](811-hardware-bus-stream-as-a-second-evidence-source.md) | WEIRD IDEA | Not proposed, not scheduled. An Ultimate 64 streams every CPU and VIC bus cycle over UDP; the word format is published (32 bits: phi2/GAME/EXROM/BA/IRQ/ROM/NMI/RW/data/addr) and matches our bus-event schema, so **stream → `.c64retrace`** is a format match rather than a stretch. With a `readmem` seed the registers fall out of the stream itself (a fetch address IS the PC), so replay into a TRX64 machine could yield a ring. The reverse direction — TRX64 emitting that word — makes any Ultimate-side tool an outside check on our VIC and bus timing, which is what went missing when the oracle was retired. **This is an instrument, not a runtime**, and §2 argues why rule 1 survives it. Unknown: the UDP framing (proprietary IP, the public module is a stub). No device here. | 2026-08-14 |
| 864 | The monitor as a library (`../TRX64/docs/864-the-monitor-as-a-library.md`) | **PARTLY BUILT 2026-09-20 — TRX64 v0.8.1** | The UE2 emulator runs the unmodified U64 firmware with trx64-core as the C64 behind its cartridge and DMA registers, and has no way to look at the 6510 — no registers, no memory, no disassembly, no breakpoints. The FFI's monitor_exec is an RPC client and the daemon's library face builds its own machine, so neither reaches a machine somebody else owns. The monitor moves into `crates/trx64-monitor` (trx64-core + trx64-static only) with the daemon as its first host: the verbs, the formatting, the cursors, the breakpoints, the observer registry and its condition AST, the flow tracker, the trap rules and the one-line assembler. The host trait keeps only what a host can answer — who advances the machine, what a mark means, where a trace goes, how a reset reaches the hardware. C64RE: **no change** — `runtime_monitor` keeps its wire contract, marked address spans and machine identity included. | 2026-09-20 **As built:** `crates/trx64-monitor` (trx64-core + trx64-static, nothing else), the host trait, `TeeObserver` moved into core, and every verb that needs only a machine; the library sees each line first, so the modal prompt and assemble mode belong to it on both hosts. **Left:** run control, reset, and each verb that needs a timeline, a file or a trace sink — their service traits are still unproven, and `CpuView` is declared but not yet on any path. A 642-line golden transcript recorded before the first line moved is byte-identical after the last. C64RE unchanged: `runtime_monitor` keeps its wire contract. | 2026-09-20 |
| 868 | A colour per pixel under turbo (`../TRX64/docs/868-a-colour-per-pixel-under-turbo.md`) | PROPOSED | The first thing TRX64 cannot render that real hardware does. Aleksi Eeben's UPic technique blanks the display and writes 384 values to `$D020` per raster line — one per VIC pixel — which a 64 MHz U64 CPU can do and a 1 MHz 6510 cannot; our VIC samples the register once per PHI2 cycle, so the picture arrives at one eighth of its horizontal detail. `turbo_phase` already is the sub-cycle position, so the CPU mirrors it, a colour store fills eight per-cycle slots, and the override applies where a colour token is RESOLVED rather than where it is placed — the 6569's one-pixel latency keeps riding the same pipeline. Gated on the U64 profile with `turbo_div > 1`: every C64 and C128 stays byte-identical to VICE, proved by the 7-game screenshots. TRX64 only; C64RE renders the frame it is handed. | 2026-09-21 |
| 814 | [Scenario recorder](_archive/814-scenario-recorder.md) | **BUILT 2026-08-20** | 812 turned around: play the run once, get the `.feature`. Writing one by hand means guessing frame counts and finding out by being wrong — while the run you were guessing about already happened, with every input on a cycle the machine knows. **The daemon stamps, never the browser**: a UI that timestamps its own click records the WS round-trip, so the replay lands somewhere else. What comes out is 813's vocabulary, not a transcript — a gap becomes a state anchor where the recorder can SEE one (text that appeared, the drive going busy then idle) and a plain wait where it cannot, and every emitted line goes through `parseFeature` before the overlay opens, because a recorder that writes its own dialect is a text generator. The `Given` is derived from the session: a mounted medium and no power-cycle since → a self-contained `Given the disk`, otherwise a snapshot — and the overlay says which, because that decides whether the file can be shared. **Every step carries who made it** (`# by: human` / `# by: llm`): the session is shared, so recording only the human's half would drop the LLM's load and break the replay two lines later — record everything, mark it, and let the editor throw out what does not belong in one click. UI: a REC button in the TOP BAR (arming is a transport state; you must see it at all times), then an overlay with an editor that validates with the REAL parser and autocompletes from the parser's own exported vocabulary — never a list typed into the browser, which is precisely the mistake the cockpit made the same week when it answered `unknown command: /turbo` for a verb the daemon had. Saves to `<project>/scenarios/`, refused while the parse is red. 812's shutter moves up into the same shape, so the bottom of the Live tab LOSES a strip instead of gaining one. **Built 2026-08-20**, all nine deliverables, `npm run smoke:814` = 39 checks — the last of which records a journal, emits the file, parses it back and REPLAYS it on a real machine to the capture it names. The anti-drift teeth are compile-time: the parser's `STEP_KINDS`/`PREDICATE_KINDS` are asserted against the `Step`/`Predicate` types in both directions, so a new step form cannot reach the parser without reaching the editor's autocomplete. Amended the same day: a key press IS recorded, as `I hold the key "SPACE" for 3 frames` — shaped like the joystick step and for the same reason. A title that scans the matrix in its own IRQ sees a key only if it is DOWN during the scan, which `I type` (a queue played out at the typing pace) cannot promise; the key name is validated against the matrix's own list. One limit stated rather than hidden: the anchor watcher reads the text screen only — in a bitmap mode it says nothing rather than inventing an anchor out of pixels. | 2026-08-20 |
| 815 | Turbo/speed registers (`../TRX64/docs/_archive/815-turbo-speed-registers.md`) | **BUILT — closed 2026-09-19** | **Closed: §3 answered for `u64` (2026-09-19, via UE2 / Gideon, 1541ultimate#665): turbo does not touch the picture, the VIC keeps its bus slots — what TRX64 does. The `128` half stays deliberately unbuilt: no C128 to measure on (owner, 2026-09-19).** A cartridge release does not ask "am I a C64", it PROBES — `$D031`, then the `$D02F`/`$D030` pair — and takes a different code path when the answer is yes. On a plain C64 every one of those reads is `$FF`, so the whole turbo half of a release (its timing tables, its raster delays) is dead code no amount of playing will reach; a bug living there is not hard to find, it is **unreachable**. Built: the registers with VICE's read-back masks (`vicii-mem.c:960-980` — write `$FE`, both read `$FE`; write `$00`, they differ by exactly `$04` — the masks ARE the detection), an extended `$D031`, and a machine profile `c64` (default, unchanged) / `128` / `u64` as BOTH a parameter (`session/turbo`) and a monitor verb (`turbo`, `turbo mode`, `turbo on\|off`, `turbo speed`). The profile survives a reset: it is machine identity, not chip state. **Deliberately NOT built — §3, and it is the half that matters:** what a SET speed bit does to the picture. VICE models the CPU side (`vicii-clock-stretch.c`) and nothing about the display, which is why the reported fault does not reproduce there either. The hardware observation — bars where text/bitmap should be while COLOUR RAM stays correct — says the addressing is fine and only the main-bus data is wrong, i.e. open bus; but whether the bars are stable or move decides the implementation, and guessing would put behaviour in this emulator that exists nowhere else. Gates: the probe run as the probe, and one asserting the picture is IDENTICAL with the bit set — so §3 stays a visible hole. | 2026-08-18 |
| 813 | [Observers and regions](_archive/813-observers-and-regions.md) | **BUILT 2026-08-18** | Waiting on a STATE instead of a cycle, and checking one. 812 anchors on absolute cycles: exact, and it fails **silently** — change the runtime and the same number lands elsewhere with nothing reported. A state anchor heals itself and cannot drift, so the state is the anchor and the cycle budget is the ripcord, at the same step. The load-bearing idea: **a marked screen rectangle is an ADDRESS SET, not a picture crop** — text mode is `$0400+row*40+col`, so a compare runs over bytes, exact and blind to sprites and raster around it, and `vic/inspect/region` already does the translation. That makes a region **the spatial mask 810 was missing**: "the intro plays" is verbal because the whole screen is noise; mark the one box and the same criterion is byte-exact — which is why 810's open item 3 waits here. Screen RAM is already characters, so `the screen shows "PRESS FIRE"` is a substring match, no OCR. A region may be defined locally in the `.feature` OR resolved from the store, local shadows store, and the run SAYS which. One new machine fact in the runtime: `session/read_memory`. Built with it: a fifth copy of the BUG-051 bank formula was hiding in `session/state`, which is why the absolute VIC bases are reported now instead of $D018 offsets every consumer has to add a bank to. The recorder that writes this vocabulary is 814. Gate `npm run smoke:813` (39). | 2026-08-18 |
| 812 | [Capture scenario + release reel](_archive/812-capture-scenario-and-reel.md) | **BUILT 2026-08-17** | A capture recipe used to say "press down, then run about two million instructions" — two seconds with the stick held, not a moment. 812 gives the schedule a clock, written in **Gherkin through 810's own parser** (widened in two directions: a scenario may start from a medium, and its `When` may be driven steps): `I wait N frames`, `I type "…{RETURN}"`, `I hold joystick 2 down for 3 frames`, `I wait until the drive is idle within 8000 frames`, `I capture "title"`. The machine emulates and C64RE drives it — the schedule runs here against a private sandbox C64RE spawns and kills, and the runtime gained exactly two machine facts (`session/frame_indices`, `session/advance_to_frame`). GIF89a comes straight from the VIC's 4-bit index buffer and its own 16-entry palette, so nothing is quantized. `runtime_scene_reel` is the door. Determinism is the gate AND the instrument BUG-050 needs. §11 records the two corrections this cost — the executor was first built inside the emulator, and our own GIF tests were green over a stream no real decoder accepts. | 2026-08-17 |

| 816 | [Sprite detection: the grid, the gate, and a number](_archive/816-sprite-detection-quality.md) | **BUILT 2026-09-05** | From issue #8. The report was about a misaligned $40 scan; the measurement said the analyzer was BLIND — Bug 27 already rejected every non-aligned candidate, so an off-phase region threw away every run it found without a word, and eight textbook sprites planted at $2400 produced zero candidates. The opposite defect hid it: `scoreSpriteBlock` summed banded terms whose FLOORS alone reached 0.49 against a 0.66 threshold, with the bands sitting exactly where 6502 code lives, so the decision rode on 0.17 of range and every new false-positive class since Spec 019 had been answered with another post-hoc demotion. Now a GATE decides and the bands only grade, and the gate measures what a sprite actually is: a contiguous figure, row n overlapping row n+1. Naive bit agreement is a trap — sparsity inflates it, which is how a table of bit masks scored 1.00 — so `shapeOverlap` divides by the union of the set bits. Byte $3F is hardware (21x3 = 63) and gates at run level. Recall 0/2 -> 2/2 at the exact planted range, false positives on pure code still 0; the intermediate states (alignment alone: 3.3 %, +padding gate: 1.1 %, +overlap: 0) are in the spec because they show the two defects are independent. Gate: `npm run measure:816`. **816.2** then built the evidence anchor the shape heuristics should not have to live without: VIC fetches sprite data through eight pointers in the last bytes of the video matrix, so `VicEvidence` gained `spriteDataAddresses` — the one field there that is NOT a register write, recoverable exactly when the image contains that screen — and the analyzer probes each named block in addition to the candidate regions, the charset-analyzer move one level deeper. A pointer outranks the padding convention for the block it names, so hand-packed data that reuses byte $3F survives; the third fixture is built exactly that way and one pointer rescues the whole eight-block run. The evidence is reported (`hardwareEvidence.spriteDataAddresses`) so a reader can tell anchored from guessed. Recall 0/2 -> 3/3. `bitmap-analyzer` got the same $2000 alignment fix, and the more useful finding is recorded rather than papered over: its heuristic scan CANNOT FIRE — on that branch `directVicMatch` is false by construction, so the reachable maximum is 0.74 against a 0.82 threshold and realistically 0.58, verified with a fixture that produces zero candidates before and after. Making it fire is a scoring redesign with no bitmap ground truth to calibrate against. | 2026-09-05 |
| 827 | [Trace storage outside the project](_archive/827-trace-storage-outside-the-project.md) | **BUILT 2026-09-06** | Issue #9: a capture is written under the project dir, the project lives in OneDrive, a play session blows past the sync quota. Measured on the maintainer's own project the same day, which makes it general: `Wasteland_EF/runtime/` holds 10.7 GB of `.duckdb` index in 44 files and 9.2 GB of `.c64retrace` log in 51 — twenty gigabytes in the very tree a user syncs and backs up, more than half of it a rebuildable index. A DuckDB file is the worst object a sync client can be handed: large, binary, rewritten continuously, no delta sync, and a client reading it mid-write can corrupt the capture. With no explicit `output` the capture now goes to a per-user DATA directory (`%LOCALAPPDATA%\c64re\traces`, `~/Library/Application Support/c64re/traces`, `$XDG_DATA_HOME`, all beaten by `C64RE_TRACE_DIR`), keyed `<basename>-<sha1[0:8]>` so two projects of the same name cannot collide. Deliberately a data dir and not a cache: the `.c64retrace` beside the index is evidence under 726.B and the system may empty a cache. The project keeps `runtime/traces.json` — the small file that SHOULD sync — and an explicit path is still obeyed, with a one-line warning naming the sync client when it lands in one. Gate `e2e:827` 43/0, hermetic: all three platform roots, the override, key stability and collision, the default landing outside the project, `resolveTraceOut` unchanged, sync detection for OneDrive/Dropbox/iCloud/Google Drive/pCloud (split on both separators, because a Windows path reaches a macOS host), append-not-replace, a corrupt pointer kept beside the new one, and a read-only project that must not fail a capture. | 2026-09-06 |
| 828 | [Reference covers RAM vectors](_archive/828-reference-covers-ram-vectors.md) | **BUILT 2026-09-06** | Issue #10: `c64ref_lookup` answers nothing for `$0291` and `$0328`, so the caller reads sta.c64.org by hand; the proposal was to have the tool fetch those pages. The gap is real, the cause is not: those are a flag and a vector in low RAM, and master's source list was ROM only — the eight `src/c64mem/*.txt` memory-map pages and the symbol table in the SAME upstream were never parsed. Verified before deciding: the maintainer's snapshot, built with 817's widened parser, holds 8 083 entries, 228 below `$0400`, including `$0291 MODE` and `$0328 ISTOP`. So 817's `parseMemoryMap` / `parseSymbols` are ported verbatim (the file is now byte-identical to the branch, so the later merge is trivial), and a miss stops being an empty line: `c64ref_lookup` reads the snapshot's own `sourceFiles[].kind` and either names the rebuild (ROM-only, and says so explicitly when the address is below `$0400`) or states the address is genuinely undocumented and points at `save_finding` for a project-specific meaning. Lookup-time fetching is declined on the record: a fact whose answer changes with the network is not a fact a workbench may serve. PETSCII/colour tables belong to the BASIC work. Gate `e2e:828` 22/0, hermetic — including that every `fetch(` sits in the build path, asserted against the code with comments stripped. | 2026-09-06 |
| 829 | [BASIC V2 tooling](_archive/829-basic-v2-tooling.md) | **BUILT 2026-09-06** (master) | Issue #11: `disasm_prg` renders a stock BASIC PRG as 6502 garbage. Measured first — it is not nothing: `SegmentKind` already has `basic_stub` and `detectBasicSysEntry` is 45 lines that scan token `$9E` in the FIRST 24 BYTES and read decimal digits, so `SYS 2064` works while `SYS(2064)`, `SYS 2*4096` and anything past byte 24 return nothing silently, and a real program is disassembled into confident nonsense. Decisions: the token table is DERIVED from the BASIC ROM keyword table at `$A09E` (which is the token numbering, and c64ref already carries that disassembly) and frozen as a constant, cross-checked against the archive.org book whose Appendix A is OCR-damaged and misses `POKE`/`LEFT$`/`MID$` — a cross-check on names, never a source, nothing copied in (817's rule for book prose); whether it IS BASIC is decided by WALKING the line-record chain and a failed walk is reported with the offset instead of half-rendered; `detokenize → tokenize` byte-identical is the gate, the same standard as the KickAssembler rebuild, because a lister nobody can invert is a lister nobody can trust; the `SYS`/`USR`/`LOAD` arguments are the actual deliverable for an RE session, with an unresolved expression reported as unresolved; control codes and colours get names from a bundled table, which closes issue #10's second half without a fetch; new segment kind `basic` beside `basic_stub`. Built: gate `e2e:829` 86/0. Two findings on the way — CRUNCH DOES suppress tokenising after `DATA` until the next colon (read at `$A5D8`/`$A59A`, so `DATA ONE` stores O,N,E and is not an `ON` token, against the widespread belief), and `basic_stub` never meant the token bytes at all: it marks the machine code a SYS jumps into, which is why three of the four consumers needed a comment rather than a change. `USR` was dropped from entry points — its argument is the float parameter to the routine at `($0311)`, not an address. | 2026-09-06 |
| 830 | [An entry point inside an operand, and the seed that ran twice](_archive/830-entry-in-operand-and-double-seed.md) | **BUILT 2026-09-07** | Two defects reported by the Neuromancer session, both reproduced against its real payload first. (1) The 6502 multi-entry idiom (`BIT` as a 1- or 2-byte skip) puts a callable, human-named entry point inside the preceding instruction's operand; the renderer names it wherever it is referenced and never defines it — 449 symbols used, 11 undefined, KickAssembler stops, and `disasm_prg` declares its own listing not byte-identical. `renderAddressAliasLabels` exists for exactly this and excludes exactly this (`!instructionOwnerByAddress.has(address)`). Fix: a DECLARED entry inside an operand splits the instruction into `.byte`, which is byte-identical by construction; a `sta` into an operand still renders `<owner>+<offset>`, so self-mod is untouched. Out-of-mapping labels (jump-table targets in another payload) become `.label name = $addr` equates. (2) `graph seed` walks the whole project for `*_analysis.json` and keys the owner off the file stem, so the `artifacts/generated/` mirror seeds every owner a second time — and since `analysis/` sorts first, the STALE copy wins silently, leaving routines without extents and every boundary/coverage number wrong. Fix: never walk the mirror, and refuse with both paths when two files claim one owner. Built: the repaired listing assembled further than KickAssembler had ever got on it and exposed a THIRD member of the same family — `emitPointerTableSegment` steps two bytes and labels only even offsets, so a split table's high half (`lda W487E,x`) is named and never defined; every data emitter that steps by more than one byte has that hole. So D3 gained a backstop over the rendered lines: every symbol USED without being DEFINED gets an equate, which emits no bytes and therefore cannot change a rebuild. Neuromancer byte-identical payloads 8/11 → 11/11 (the three reported), Wasteland_EF unchanged 14 identical / 4 pre-existing red. Gates `e2e:830` 14/0 with a real KickAssembler rebuild, `e2e:830-seed` 10/0, `smoke:741`/`e2e:741` still green. Verified independently by the reporting session (it assembled the listing itself: 0 errors, 0 warnings) and merged to master. **830.1** (§6), reported by the same session after the merge: an instruction may not cross `segment.end`. D1 resumed a decode at a declared entry without carrying the segment bound into it (`ff ff a4` at `$6E9C`, segment ends `$6E9D`), so the next segment re-emitted a byte and the rebuild came out one long; bisecting by assembled label address found a second, OLDER instance with the same shape (`cpy` at `$6AA2`, segment ends `$6AA3`, reached through the inline-string resume). Clamping the decode at the segment end makes Neuromancer 11/11 byte-identical at 694 annotations and lifts Wasteland_EF from 14 to **16 of 18** — two of its four long-standing failures were this defect all along. `e2e:830` 19/0. | 2026-09-07 |
| 831 | [A dump lands in the project, and the button says so](_archive/831-dump-lands-in-the-project.md) | **BUILT 2026-09-07** | The `⬇ Dump` button called the daemon with a RELATIVE path, so four `.c64re` machine-state files (1.4 MB, 334 KB ×2, 58 KB) sat in the tools repo's gitignored `dumps/` — Neuromancer's and Wasteland's state, in a directory belonging to neither. Silent in both directions (success `console.log`, failure `console.error`), which is why it went unnoticed for two months, and `onSnapshotTaken()` sat outside the `try` so a filmstrip frame appeared whether the dump worked or threw. Decisions: `<project>/runtime/dumps/` — Spec 827 moved TRACES out because a DuckDB index is rewritten continuously while a sync client watches, and a `.c64re` is the opposite, written once and never touched; the path is computed on the C64RE side (a library with a gate, one route, then a button — rule 6) and handed to the daemon ABSOLUTE, because the daemon's cwd is not the project's; the button reports path, size and failure in the DOM; a failed dump takes no screenshot. Built: `e2e:831` 35/0 in `gates.yml`. Proven end to end without a browser — the route's absolute path handed to the live daemon returned `fileBytes: 1471326` and the file appeared under the project's `runtime/dumps/`. Seen on screen: the control bar reads `⬇ 1455 KB · runtime/dumps/dump-…c64re` and the file is there — the size leads because the bar is tight, so the ellipsis eats the nonce and never the fact that it worked. | 2026-09-07 |
| 832 | [Six defects the Ultima VI session found](_archive/832-what-the-tools-claim.md) | **BUILT 2026-09-09** | Eight reports from the Ultima VI session; six defects, two model gaps recorded and not built. The thread: a tool that knows something and produces an artifact that does not say it, or says what it never knew. A routine annotation renames an existing label but never creates one (22 of 39 names reached the listing); a directory entry's first two bytes become a load address unconditionally, so a record file claims `$FFxx` and one CHECK violation discards the whole manifest import; `importManifestKnowledge` gates on `artifact.role` so a manifest this repo wrote cannot be re-imported by it; `decodeGCRTrack` accepts a pair on `gcrValid` alone and emits sectors whose header checksum failed — plus 256 invented zero bytes when the data block is unreadable; the audit counts 4 101 tool-written sector dumps as human debt; and a loader-entrypoint slug keeps an address that was corrected. Built by four agents, one class each, in separate worktrees; three corrected the spec. Measured: annotation names reaching the listing 5/8 -> 8/8 on the fixture; GCR sector counts on eight real images now equal the firmware-style scanner exactly (dungeon 684->683, Pawn 701->683, LN3 697->683, five unchanged) with **5 632 invented bytes** gone and the custom-CRC sectors still extracted at full width. The D4(b) wording would have broken the tolerant reader if followed literally — gating on `data.gcrValid` takes Pawn from 19 sectors to 0, because the 325-byte read overshoots the block and group 64 fails routinely; the line belongs at 'no data block at all'. Report 3 is decided, not deferred: the LUT model expresses one addressing class and is not widened — only the refusal changes, and it must not describe the target shape. Gates `e2e:832-annotations` 17/0, `-disk` 37/0, `-gcr` 33/0, `-ids` 37/0, `-lut` 15/0, all in gates.yml. | 2026-09-09 |
| 833 | [A tool may not claim what it did not do](_archive/833-a-tool-may-not-claim-what-it-did-not-do.md) | **BUILT 2026-09-09** | 832's thread, one layer out: five more tools asserting what they did not do. `disasm_prg` without `analysis_json` never indexes the annotations file it found and prints "Semantic annotations applied" anyway (measured: 1 250 W-labels, 0 of 8 names); a segment annotation's label never reaches `labelsByAddress` at all; a graph-import line reads as a rendering result and caused the original misdiagnosis; `.invalid` in a sector filename marks the data checksum, so on a custom-CRC disk it marks every real sector, and since 832 it marks nothing worth avoiding; and `sandbox_6502_run` resolves its project with no hint, depending on the process cwd — which the path-portability gate missed because the paths are nested in an array. Built by three agents. Render fixture: without `analysis_json` 0 of 8 in-image names -> **8 of 8**; with it 8 of 9 -> **9 of 9**; byte-identical in both modes. Widening the portability gate to walk nested schemas showed the hole is **15 default tools** wide, not one — the six headless runtime tools, the seven trace-store readers, `runtime_scene_reel`, and `sandbox_depack`, which declares `project_dir` and ignores it. None fixed; frozen in a KNOWN_HINTLESS allowlist that can shrink and not grow. Dropping `.invalid` made two pairs claiming one (track,sector) compete for a filename — resolved by best `dataStatus`, loser keeps a `path: null` entry naming the winner; zero collisions on five real images. Closed out with D6/D7, the two instances of its own shape found during the build: two **explicit** `labels[]` entries with one name at two addresses both got a definition and broke the rebuild in the assembler — the only item in this family that goes red rather than merely misleading — and the wrapper's annotation check looked in one place while the renderer looks in three. Gates `e2e:833-render` 49/0 (6 failures with the D6 guard removed, including the KickAssembler rebuild), `-sectors` 35/0, `-sandbox` 28/0. | 2026-09-09 |
| 834 | [Fourteen tools that cannot find their project](_archive/834-fourteen-tools-that-cannot-find-their-project.md) | **BUILT 2026-09-09** | Spec 833 widened the path-portability gate to walk nested schemas while hunting one tool, and found fifteen: of 70 default path-taking tools, 15 hand `resolveProjectDir` no hint, so the root comes from `C64RE_PROJECT_DIR` or from the process cwd — fifteen violations of the rule written in the tool-matrix header. Three shapes: the six headless runtime tools miss SILENTLY (every call try/caught, `prg_path`/`media_path` unused); the seven trace-store readers fall back to `process.cwd()` explicitly, which after Spec 827 answers a question nobody asks any more; `runtime_scene_reel` fails outright. `sandbox_depack` — the fifteenth, which declared `project_dir` and ignored it — is fixed in this spec and has left the KNOWN_HINTLESS allowlist. Decisions: every tool takes a hint from a path the call already carries, the trace-store cwd fallback is removed rather than re-pointed, the headless six stop swallowing the resolution error, and the allowlist empties so the rule has no exceptions. Built by three agents. KNOWN_HINTLESS is **empty**; gates `e2e:834-headless` 118/0, `-trace-store` 93/0, `-scene-reel` 41/0. The build moved the count twice: two of the six headless tools resolved NO project at all (the PNG landed against the MCP process cwd, the dump path went raw to a project-agnostic daemon serving several projects), and `runtime_trace_start` is a SIXTEENTH instance the gate could not see because its path parameter is called `output` — now fixed, with the walk's name matcher widened. For the trace-store seven the hint turned out to be nearly a formality: an absolute path needs no project and a relative one is equivalent to hintless, so the load-bearing fix is a probed candidate search over `<project>/`, `traceDirForProject()` and 827's pointer file, neither of which is sufficient alone. Found and not fixed: `absStorePath()` in `trace-read.ts` keeps a cwd fallback that the workspace-UI trace routes still reach. | 2026-09-09 |
| 835 | [A tool must say what it can do](_archive/835-a-tool-must-say-what-it-can-do.md) | **BUILT 2026-09-09** | The mirror image of 832–834. A project session could not work out how to start a runtime session from a cartridge; `runtime_session_start` has taken one through `media_path` since BUG-041, and nothing said so — `media_path` was `z.string().optional()` with NO description (the explanation sits above it as a source comment an MCP client never sees), the tool's text said "C64+1541" and never mentioned a cartridge, the only visible medium name was the deprecated `disk_path`, and `docs/tools/headless.md` mentioned CRT once, for swapping one in a session already running. 3 of 15 parameters were described. Now all 15 are, the description names the cartridge, PRG and snapshot cases and the content rule, the deprecated alias explains that its own name is the misleading part, and the doc shows the call. The "sandbox" word collision — a CPU sandbox with no machine vs an ephemeral machine — is documented rather than renamed, because renaming a tool breaks every caller for a cosmetic gain. `e2e:835` 12/0. | 2026-09-09 |
| 836 | [A machine of your own](_archive/836-a-machine-of-your-own.md) | **BUILT 2026-09-09** | A project session tried to start a headless runtime with its own cartridge and got the human's live session instead. Three findings: `runtime_session_start` calls `media/open` even when it ATTACHED, so a second caller's `media_path` swaps the medium under the human using that machine; no tool says which machine it touches, so there is no way to learn which door is the private one; and the private machine already exists and has exactly one caller — `src/reel/sandbox-session.ts` does what doctrine rule 2 describes (own port, child process, budget, self-ending) and is reachable only by expressing your intent as a `.feature` file. Decisions: an attach refuses a medium and names the dedicated door; every runtime tool states whose machine it is; and one new tool exposes the private machine without a scenario file, deliberately without a session_id that outlives the call — a sandbox you can attach to later is a second shared machine. Built: `runtime_sandbox_run` over the existing `sandbox-session.ts`, which was not changed — its budget-and-die contract is what makes starting one safe. The interactive line: a call may express anything complete in itself, nothing whose value depends on a later call, so a run-until is in and a `session_id` is out. "Needs no work in the runtime repo" was right for a cartridge and a disk and **wrong for a bare `.prg`**: `media/open` sets `session.injected` and the runtime's `full_machine_gate` then runs an isolated CPU with no VIC, CIAs, SID or drive — measured, raster frozen and PC parked at `$E5CD`. The tool detects it and says `NOT A WHOLE MACHINE`; the fix is the runtime's and is open. Also fixed here: `resolveDaemonSpawn` looked at `repoRoot/../TRX64`, which from a git worktree is `.claude/worktrees/TRX64` — so every agent in a worktree got a false "no runtime" skip for a binary sitting right there. Gates `e2e:836-attach` 11/0, `e2e:836-sandbox` 65/0 hermetic, 80/0 with the binary. | 2026-09-09 |
| 837 | A paused machine still has a picture (`../TRX64/docs/_archive/837-a-paused-machine-still-has-a-picture.md`) | **BUILT 2026-09-10** | Issue #13 — a cockpit stuck on "No frame yet" after an `undump`, never recovering, while every other reading said the machine was healthy; the only known cure was killing the daemon and losing the session. Cause: the runtime renders and broadcasts inside `if running`, so a PAUSED machine sends no frame at all, and the stream loop starts on the first subscriber — a client that reloads during a pause starts a loop that renders nothing. The one-shot present already existed (2026-07-15 regression): every OPERATION that leaves a paused machine sets it, nothing set it on ARRIVAL. `subscribe()` sets it now. The reporter's guess (the RAM/cart divergence warning) was wrong — that warning is correct and harmless. C64RE half: the workbench already knew the stream had stopped (fps zero while the run-state says running) and said nothing, so a dead picture looked like a slow boot; it now says which state it is in and offers the way back. | 2026-09-10 |
| 838 | [Three field reports](_archive/838-three-field-reports.md) | **BUILT 2026-09-10** | #15: an entity id (`slug:ram/loader:payload:0801`) is used verbatim as a directory name, so `mkdir` fails on Windows and the doctrine guarantee 'no raw extract without a disassembly' silently does not hold there. #17: the sandbox harvest returns a contiguous window with no signal for which bytes the routine wrote, so un-written bytes come back as BASIC/KERNAL ROM residue, indistinguishable from payload — Spec 832 D4 with different bytes, and the write set already exists. #16: code entered only from another overlay renders as a `.byte` wall, and the obvious workaround makes it worse, because `entry_points` constrains the scan rather than adding to it. Built by three agents, all of which corrected the spec. D1 was six id-to-path sites, two of them an ESCAPE rather than a portability bug (a single-letter slug makes `c:ram/…` a legal id that `path.win32.resolve` follows out of the project; an LLM-supplied `run_id` went straight into a path), and a DOS device name cannot be fixed by a suffix — Windows matches up to the first dot. D2's CLI half was never broken, and the real defect was one level deeper: `writtenSpan` gap-filled with ZERO between disjoint runs — 832 D4 in a second place. A gap is now an explicit `null`, because `$00`–`$ff` is the whole domain of a byte. D3 answers (a) with TELL rather than split — honouring an entry inside an instruction means two decodes and only one can be emitted — and on the reporter's own project 3 of 190 entry points were being dropped in silence. Measured: Neuromancer `chunk_4300` **1 111 → 8 930 code bytes**, rebuild byte-identical; twelve payloads +11 295 bytes; unchanged byte-for-byte with no graph present. Gates `e2e:838-paths` 165/0, `-harvest` 63/0, `-islands` 51/0. | 2026-09-10 |
| 839 | [What the human can do, the LLM can do](_archive/839-what-the-human-can-do-the-llm-can-do.md) | **BUILT 2026-09-10** | Issue #18 plus the instruction that followed it. The first measurement compared WS verb strings and was wrong: the MCP reaches the machine through `monitor/exec` and `api/call`, so an absent string is not an absent capability, and `runtime_media_unmount` does not LACK the cartridge case — it refuses it. Re-measured by capability against the daemon dispatch + `monitor_help_text()`: eleven UI verbs are already reachable (reset/power/warp/transport via the monitor, audio start/stop are acks with no ears to hear them), and six are not — cartridge eject, drive+cart status, drive cold-re-init, VIC region/origin (Spec 721's join beyond one pixel), a trace built from the ring after the fact, and the daemon's recent-media list. One defect of the same family found while measuring: `runtime_session_status` claims "both CPUs, IEC bus, drive" and returns one CPU, and `runtime_monitor` never names reset/power/warp/marks/play/rstep/whowrote/triage — Spec 835's invisible reach. The owner reshaped it mid-build: the `/` verbs should be DAEMON commands, not cockpit commands — and that was already the built doctrine, with three verbs exempted because "the media handlers are a 170-line block that has to be extracted first". They never needed extracting: `dispatch` is callable in-process. So the capability lands ONCE, as seven forwarded monitor verbs (`mount`/`eject`/`drive`/`cart`/`drivepower`/`recent`/`tracering`), intercepted before the state lock because `run_monitor` holds it and re-entering would deadlock; cockpit, web UI and `runtime_monitor` got them at the same moment, and the cockpit lost its last two machine verbs plus a `session/play` reconcile that cut a future the power-cycle had already discarded. C64RE keeps only its own: the `role` guard (#18), the status tool that claimed "both CPUs, IEC bus, drive" and returned one, the monitor description that named none of this, and two VIC tools whose node lists text would flatten. Building it found the daemon's own advertised-verb gate matching one of two "unknown" wordings and counting six lines of help PROSE as verbs. Delta now 61 UI / 66 MCP / 24 UI-only, all 24 explained by name and reason in an allowlist that may shrink, not grow. | 2026-09-10 |
| 840 | An empty expansion port is not RAM (`../TRX64/docs/_archive/840-an-empty-expansion-port-is-not-ram.md`) | **BUILT 2026-09-10** | Found while reading VICE to scope issue #19 (REU emulation), and older than that request. `$DE00-$DFFF` with no cartridge fell back to the write-through I/O shadow, so an EMPTY expansion port behaved like RAM: write `$55`, read `$55`, for ever. Hardware detection is conventionally "write a pattern, read it back, compare" — so every probe for a cartridge, a freezer, a GeoRAM or an REU was told the device is present on a machine with nothing plugged in. A real C64 has the open bus there: the VIC's last phi1 fetch, raster-dependent. The port is one line — `viciisc/vicii-phi1.c:34` `return vicii.last_read_phi1;` — and TRX64 already maintained the field, it just never consulted it. FOUR sites: the CPU read, the side-effect-free peek, and both monitor lenses; the three peek paths are BUG-049 repeating, where a readback showed the last value written and hid the evidence it was meant to provide. This inverts #19's own diagnosis ("open bus, the probe result is garbage") — it was not garbage, it was a perfect echo, and the cruncher drove a device that was not there. Whether that binary now takes a clean no-REU path is a measurement for the reporter. Gate writes two patterns (one can match the bus by luck) and asserts monitor and CPU agree; verified red without the fix. | 2026-09-10 |
| 841 | [A keyset belongs to the game](_archive/841-a-keyset-belongs-to-the-game.md) | **BUILT 2026-09-10** (owner testing) | Ultima VI needs keyboard AND joystick at once, and the keyboard-joystick was hardcoded to WASD in three places. The owner set the architecture: "trx64 nimmt die Keys/Joy an, wie verbs. Der Client bringt sein mapping mit" — the daemon takes actions (`session/key_down`, `session/joystick_set`) and must not learn about host keyboards, so **TRX64 is not touched** and each client owns its own map. An earlier draft had the daemon serve the table to both front-ends; that put a client concern in the server and would have left a keyset unreachable on a standalone TRX64, and is recorded as declined. Three levels merged PER BINDING — built-in default → `~/.config/c64re/input.json` → `<project>/runtime/input.json` — because a project that only moves the fire button must keep following the global for the rest. The model had to grow: `KeysetBindings` holds five joystick directions and cannot say "F1", but moving the stick off the letters a game types is exactly the U6 fix, so a binding is now `C64 action ← host key` where an action is a joystick bit OR a C64 matrix key. Most of the parts existed and none were connected: the config schema (Spec 264), an editor panel that was never mounted, and `registerInputHandlers` defined and never called. The dialog is a plain list — "die Liste ist genug, keine chichi Optik" — with a provenance column, a scope switch that is a target rather than a filter, and a warning line naming the keys the joystick has swallowed. Gate `e2e:841-keyset` 31/0 with the levels injected. | 2026-09-10 |
| 842 | [Two addresses for one byte](_archive/842-two-addresses-for-one-byte.md) | **BUILT 2026-09-11** | Issue #20 — `disasm_prg` with `relocations` and `segments(kind:"data")` do not compose: the island renders as an EMPTY `.byte` outside the `.logical` block, the same bytes are decoded again as code inside it, and the stream after the island misaligns. The mixed code/data model inside a relocated block already exists (Spec 741 §2a `renderRelocationBody`/`RelocationSubSegment`, whose bounds are already runtime-space) — `subSegments` is simply a caller-supplied field that nothing ever derives from the annotation file, so the segments take the ordinary path, get their file range subtracted by `subtractRelocations`, and come out empty. Two features built a spec apart and never introduced. The owner settled the address-space question #20 raises: BOTH, and the graph is keyed on RUNTIME — "wo sie im Payload sind ist ok, aber wichtiger nach dem reloc (pseudo pc) ist das ja der Ort, wo es zur Laufzeit ist, das muss der Graph kennen". So every addressable annotation may state `space: runtime|file` (runtime the default under a relocation), both are always resolved and kept, the walk re-cuts at `segment.end + 1` (838 D3 one level down, which the reporter names himself), and `[annotations] applied 14, skipped 0` — the line that made this bug look like a success — starts reporting what actually landed inside the block. Built, and the spec was wrong twice: the outer path DID need a change (a segment lying outside the file image has no bytes, and rendering it IS the empty directive), and D1's promise of identical output needed the space resolved at INDEX time rather than render time, because runtime annotations also feed the segment map and the external-label equates. Gates `e2e:842-reloc-data` 20/0, `e2e:842-graph` 11/0; 741, 838 and the byte-exact rebuild unchanged. | 2026-09-11 |
| 843 | [Click a pixel, name the thing](_archive/843-click-a-pixel-name-the-thing.md) | **BUILT 2026-09-11 — merged 2026-09-19, TRX64 0.8.0** | The owner, on the frozen Live screen: "ich wollte gerne im Freeze-State egal wohin klicken können, sehen woher die Pixel kommen und Annotationen machen". Two agents read the Inspect overlay and the VIC block. Seven defects, four gaps, and one root: `"vicProvenance": Value::Null` is hardcoded at the capture site, so the per-line record that exists as a type is never written and every answer on a raster-split screen resolves against the frozen 8 hardware registers — which is exactly his screen (bitmap plus a text box). Also: a scrub leaves the inspected checkpoint behind silently, `Promote → Knowledge` writes an artifact and says it wrote knowledge, the pins leak until the ring cannot capture, a region drag renders empty rows, and `Resolve origin` cannot succeed on any real medium because it hashes the raw image at fixed strides. Interviewed: his four wants (tell sprite from bitmap, annotate areas into the disassembly, rip, inject) are ONE chain, and the unit is the BYTE RANGE — the rectangle is only the pointing gesture. Injection is a runtime candidate patch, "wie Docker-FS mit Overlays", never a media rewrite. His own correction: the round-trip belongs in the graph, not in a file header. And the graph is the truth for MEANING while the annotations file is PROJECTED out of it — a listing has to stay a pure function of files in the repo, because byte-identical rebuild is this repo's verification standard. | 2026-09-11 |
| 850 | The expansion port as a device interface (`../TRX64/docs/_archive/850-the-expansion-port-as-a-device-interface.md`) | **BUILT 2026-09-15** | UE2 (the Ultimate firmware emulator, `u64-emulator`) needs TRX64 to deliver the Ultimate Command Interface: register bytes in `$DE00-$DFFF` whose reads have side effects, an IRQ, a freeze that holds the 6510, a `$FF00` trigger and a `$D038`/`$D036` unlock. Its requirements R1–R6 are all generic, so TRX64 builds an interface, not UCI, and nothing on it is a cartridge: a device place for the machine profile and one for a host, side-effect-free peeks (fixing TRX64's own uncalled `CartMapper::peek`), a device stop reason, a banking-independent write snoop (VICE's `$FF00` REU hook, which TRX64 claimed and never had), one expansion interrupt source (VICE's one-source-per-device shape instead of the two asked for), and CPU hold as a run state with a reset flavour. Stretched reads: the machine reports the stall, the device decides what it means. The owner ruled out the fake-cartridge workaround; the first device is TRX64's own UCI block (852). BUILT the same day: gate 16/16, full TRX64 gate green, and the one number that mattered — the first build cost a stock machine 6 % against a claim of "one predicted branch", and now costs nothing measurable because every port step sits behind one flag per run. A badline read measures 43 stolen cycles, 3 of them on the bus. | 2026-09-15 |
| 851 | The U64 machine profile, and a CPU that is actually faster (`../TRX64/docs/_archive/851-the-u64-machine-profile-and-a-faster-cpu.md`) | **BUILT 2026-09-16** | The owner: "beim Start des TRX64 einen Parameter, Default C64, optional U64/UE2/128" — the mode is simply "I emulate an Ultimate, or I don't". `--machine c64\|u64\|128` before power-on; `u64` (also `c64u`, `ue2`) is one machine for U64, Elite II and C64 Ultimate, which differ only in the speed table. The turbo registers take the firmware's own layout — speed index plus badline bit — which exposes an 815 defect: `$D031 = $80` (1 MHz, badline timing) reads as turbo engaged. A real speed-up by a clock divider ported from VICE's TurboMaster, so `clk` stays the PHI2 clock everything is keyed on; the cost is the host's business, the only rule is that 1 MHz costs nothing. | 2026-09-15 |
| 852 | The Ultimate Command Interface as U64 hardware (`../TRX64/docs/_archive/852-the-ultimate-command-interface-as-u64-hardware.md`) | **BUILT 2026-09-16** | The owner decided UCI lives in TRX64, not in UE2 — "UCI ist doch für alle U64 und C64U gleich", and it is: `command_intf` is built for every Ultimate firmware target against one open VHDL entity. `command_protocol.vhd` (313 lines) ported one to one into the `u64` profile: C64-side registers and read side effects, IRQ, freeze as 850's hold, the `$FF00` trigger through 850's snoop, the firmware side as an API a host maps onto `CMD_IF_BASE`, plus the two events the firmware needs and cannot see itself (a C64 reset, the `$D038`/`$D036` unlock). Disabled without a firmware — the firmware's own default — so standalone TRX64 shows open bus. UE2's planned model of the block is not built; S15's steps 1-2 disappear. BUILT the next day: `uci_gate` 15/15, full TRX64 gate green; the block survives a C64 reset, is in no snapshot and comes back at power-on from every restore; a badline-stretched read advances the pointer 4 times (3 cycles on the bus + 1), not 44; a stock C64 pays nothing measurable. | 2026-09-15 |
| 853 | The REU and GeoRAM: a device that drives the bus (`../TRX64/docs/_archive/853-the-reu-and-georam-a-device-that-drives-the-bus.md`) | **BUILT 2026-09-16** | Issue #19 asked for the REU; 840 had already fixed the defect it reported (an empty port echoed writes, so the cruncher found a device that was not there), so this is the feature, not the bug. The first device on the port that **drives** the bus instead of answering it — 850 built the hold and left the bus out on purpose, and that DMA engine is the weight of this spec. Ported from VICE's `reu.c` exactly, the parts that read like mistakes included: the 1700's own wrap, the "hacked REU" above 512 KB where the REC chip still wraps at 512 KB, the Half-Autoload-Bug, and verify's three documented weirdnesses. Owner decisions: a CORE device (on `c64`, not only `u64`); the RAM **out of the checkpoint ring** and **in the `.c64re` dump** (16 MB in a 32 MiB / 64 KiB ring would not shrink it, it would destroy it) — the split is an option on the one capture, following 807's framebuffer line; no write-back to disk, an optional load only; GeoRAM in scope as the opposite cost (a 256-byte window, two write-only registers, no DMA, 723 lines against 1688). A restore that cannot cover the expansion RAM SAYS so (792's lesson: the silence is the defect), and an absent node leaves the device alone rather than ejecting it the way a missing cartridge node does. 850's two device places became a list, VICE's IO Slot shape, with no core surgery — a device that holds several satisfies the same trait. Reachable: `--reu` / `--georam` / `--reu-image`, monitor verb `reu`, `session/reu` with a RAM window. Gate `reu_gate` 31/31 through the CPU bus; full gate green (98 gate tests, daemon 382, seven games 7/7); `perf_bench` 11.373 MHz median, inside the band 851 and 852 measured. | 2026-09-16 |
| 854 | The expansion RAM the host owns (`../TRX64/docs/_archive/854-the-expansion-ram-the-host-owns.md`) | **BUILT 2026-09-16** | Out of UE2's integration of 853, which was green and then named the one thing 853 §5 had left to the bridge: on the U64 the REU's RAM IS the firmware's DDR (`REU_MEMORY_BASE 0x1000000`), and the firmware preloads an image by writing there with its own CPU — so a device that allocates its own 16 MB gives two copies, and every preload lands in the one the C64 never reads. **Additive: 853 stays closed.** Owning the RAM is right standalone and is what makes a `.c64re` dump round-trip; 854 adds the borrowed store, the shape the bridge already uses for the cartridge (`lend_ddr`). The store is a trait the device HOLDS — GeoRAM decided that, because it reads its RAM on every `$DE00-$DEFF` access and not only during a transfer, and a `'static` device cannot hold a `&mut [u8]` without putting a lifetime on `Machine` and every call site in the crate. "Nothing lent" needed no new concept: it reads the floating-bus latch and drops writes, exactly as 853 already treated an address with no DRAM behind it, so a bridge that lends only around bus accesses is in the normal case between them. A borrowed store is in no snapshot — those bytes are the host's memory image — and `expansion_ram_uncovered()` already said so. The cost was 38 call sites (10 production, 28 gate fixtures); a regex pass did them and the compiler caught exactly the two it could not, the `usize` loop indices. No daemon flag: `--reu` still means owned, and the borrowed mode is an embedding API for a host running the core in-process. Gate 38/38, full gate green (105 gate tests, daemon 382, seven games 7/7). | 2026-09-16 |
| 855 | More than one SID (`../TRX64/docs/_archive/855-more-than-one-sid.md`) | **BUILT — closed 2026-09-19** | **Both halves done: UE2 confirmed its half (UE2 0.3.0, `cbbae9e`; S17-ultisid) — map from the firmware decoders, one reSID per receiver, mixer, readback through the host door; stereo, filter curves and socket 2 deliberately unbuilt.** **TRX64's half is COMPLETE: D1–D6 built and gated (`sid_multi_gate`, sixteen cases), D7 resolved without code, D8 in the `.c64re` snapshot. Still PARTLY BUILT because UE2's half is unstarted.** A U64-II decodes four SID targets — socket 1, socket 2, UltiSID 1, UltiSID 2 — whose addresses the firmware assigns at runtime and rewrites on every C64 reset; TRX64 has one, at `$D400`, mirrored by a `& 0x1f`. The blocker is OURS, not reSID's: `resid_shim.cc:34` holds a single `SID g_sid;`, the twenty `extern "C"` functions take no handle, and `resid_ffi.rs` enforces it with a process-wide `RESID_GUARD` held for a `Resid`'s whole lifetime — so a second `Resid::new` does not run slowly, it **deadlocks**. reSID itself is stock and instantiable (VICE builds one per chip, `resid.cc:94`), so most of this spec removes a restriction we imposed. **The API was negotiated with the UE2 session and closed on both sides before either wrote a line of code** — the interface was agreed, not discovered by one side and absorbed by the other. Six pieces: handles in the shim (old names as handle-0 wrappers, so `resid_oracle` and c64re byte-identity survive untouched, and no vendored GPL source moves); a decode table the host RESOLVES and the core CONSUMES, with no VICE defaults baked in and `cold_reset` forbidden from clearing it (the firmware rewrites it on every reset, and a self-clearing table would race that); one `Sid6581` per chip, because `$D41B`/`$D41C` are the 6502's view and not audio; a write trace carrying `(chip, reg, value, clk)` where today it carries neither address nor cycle; a host `read` PLUS a side-effect-free `peek`, both `Option<u8>` with `Some` winning, in 850's shape — the peek is not garnish, without it the monitor shows our shadow while the C64 gets the host's answer; and a read-only envelope getter from the fastsid model that already answers `$D41C`. Two things are recorded rather than approximated. **Two reSID instances ARE sample-synchronous** — `sid.cc:982`, the cadence comes from `clock_freq`/`sample_freq` alone and is content-independent; I claimed the opposite twice, to this repo and to UE2, and it is written into the spec so nobody rebuilds the design around it. And **UltiSID is not a plain 6581/8580**: `WAVES` maps onto the reSID model, but the resonance switch, the digi level and a 1024 × uint16 filter curve have no reSID equivalent, so TRX64 models none of them and says so — an invented filter curve would be this week's UCI defect with better manners. Open, deliberately: where a SID mapped into `$DE00-$DFFF` sits against the expansion chain, which it shares with UE2's sampler at `$DF20+`, the UCI and the REU mirror. Both sides believe the FPGA decodes ahead of the cart slot and neither can show it — the U64-II top level is not in the open firmware tree and the open U2+ top has no UltiSID — so precedence is a per-entry bool with no default. That is the 2026-09-17 lesson applied: the UCI pointer defect was an unverified assumption whose gate confirmed it, because the test came from the same assumption. | 2026-09-17 |
| 856 | Turbo pays per instruction (`../TRX64/docs/_archive/856-turbo-pays-per-instruction.md`) | **BUILT 2026-09-17** | The owner on the UE2 emulator: the core holds at 1 MHz and collapses as the MHz setting rises. Turbo is CPU-only (851 D3), so 64 MHz is 64× the 6502 work — the price of a software CPU. What was charged on top: the run loop synchronised at every INSTRUCTION boundary, and UE2's profile of UltimateDemo2026 at 64 MHz (M4, REU+UCI, 0.72–0.98× real time) put that boundary work at ~40 % of the thread. Built: instructions run back to back inside one bus while `clk` has not moved AND nothing but RAM was touched — the second condition because within one PHI2 cycle the boundary restamp is the only path an IRQ acknowledge has, and without it every handler storms. Measured on a RAM loop at 64 MHz: 0.86× → 1.40× real time (rings off), 0.77× → 1.09× (rings on); an IO-touching loop 0.79× → 1.07×; 1 MHz unchanged. **Confirmed on the demo by UE2: every window now holds real time (1.000 / 0.999 / 0.999 / 0.997, process CPU 69–97 %), and the owner heard the audio underruns stop.** The re-profile shows loop self 15–17 % → 5–11 % and expansion line polls 8–9 % → 2–5 %, while the CIA timer update barely moved (16–18 % → 12–16 %) and is now the largest single item — its cost was the real per-PHI2 advance, not the repeats. `turbo_fastpath_gate` (5 cases, in gate.sh): acknowledge-once for CIA and raster, and exact equality fast path off vs on over instructions, bus records and interrupts — both failure kinds provoked by mutation before trusting it. Building it found one thing the spec had not named: the bus's pre-fetch state (`fetched`) must be reset per batched instruction, or its fetch is attributed to the previous opcode — invisible in machine state, wrong in every trace. Named limits: cartridge-ROM code gets no fast path (the flag is conservative); rings stay per instruction; `DeltaRing::commit` is still an out-of-line call when disabled (~1 %). | 2026-09-17 |
| 857 | The CIA alarm is a comparison, not an update (`../TRX64/docs/_archive/857-the-cia-alarm-is-a-comparison.md`) | **BUILT 2026-09-17** | Found by UE2's profile after 856: the CIA timer update was the largest item left. VICE checks alarms with a comparison in every prologue and every cycle; our `process_alarms` ran the full register-access catch-up there (~36 per PHI2 cycle at 64 MHz). Measured, alternating A/B 9/9: 1 MHz booted 10.53× → 13.33×, 64 MHz RAM loop 0.97× → 1.22×. Closed first: Timer B's alarm was predicted and never fired, a cascade step did not re-predict it, and readers relied on the per-cycle catch-up. The spec's first claim that the `.c64re` restore needed re-arming was wrong. The gate's frozen digests found an older defect: checkpoints did not carry the turbo phase (since 851), fixed separately. **UE2 confirmed it on the demo:** process CPU per window 76/85/69/97 % → 66/74/59/81 %, `Ciat::update` gone from the profile (15.9 % → 0.0 %), 435 workspace tests plus cartridge smoke, uci-targets and mandelbrot identical with the check off and on. Merged to TRX64 `main` and released in 0.7.3. **Measured against the previous release afterwards, alternating against a `v0.7.2` worktree: +18 % warp throughput on a stock 1 MHz machine (4/4 pairs)** — the +27 % figure in the spec's own table is the switch inside one binary, not a release comparison. With the check switched OFF the tree is ~6 % BELOW 0.7.2 (3/3), which is D2's cost and confirms what UE2 saw: the kill switch restores behaviour, not speed. | 2026-09-17 |
| 858 | [The daemon serves the project you are looking at](_archive/858-the-daemon-serves-the-project-you-are-looking-at.md) | **BUILT 2026-09-18 — TRX64 0.8.0** | The owner, in the Ultima VI EasyFlash workspace: the CART `[insert ▾]` picker was empty. Not a regression — the daemon on 4312 had been started five days earlier for another project, the workspace attached to it, and `media/recent` lists only media inside the DAEMON's project. Nothing compared the two. The owner decided: correct the daemon, but only after a requester, and move it in place rather than restart it. TRX64: one project source instead of seven copies reading the process arguments; `ping` names the project; `project/set { path, dry_run? }` writes back and ejects the media, clears the old session, switches, power-cycles and broadcasts `project/changed`. C64RE: a requester at the workbench root (runtime project, this workspace, what is mounted, what switching ends; switch or leave, leave collapses to a bar), the pickers reload on `project/changed`, and `runtime_session_status` reports a `projectMismatch` but never moves the runtime — that ends the human's session and is the requester's decision. `npm run smoke:858` 13/13 against its own sandbox daemon; TRX64 daemon suite green. Not covered: the refusal on non-persistable dirty media (no fixture mapper). | 2026-09-18 |
| 859 | [The line as the VIC saw it](_archive/859-the-line-as-the-vic-saw-it.md) | **BUILT 2026-09-18 — merged, TRX64 0.8.0** | The owner, on [vicspector](https://github.com/elysium64/vicspector): "eine solche Sicht wie vicspector, aber halt mit den echten Daten der Emulation". vicspector computes one raster line from Bauer and Åkesson; this draws it from what the core did. TRX64 records (a per-cycle recorder armed only in a scratch replay, `vic/line_trace`); C64RE shows (the line view under the clicked pixel in the Inspect overlay, `runtime_vic_line_trace`). The replay runs in a CLONE from the latest ring anchor and is checked against the picture on screen (`verified`). On the 843 branch by the owner's call. Gates: `vic_line_trace_gate` 6/0, daemon test, `smoke:859` 11/0. | 2026-09-18 |
| 860 | [The frozen frame as a view](_archive/860-the-frozen-frame-as-a-view.md) | **BUILT 2026-09-19 — merged, TRX64 0.8.0** | The owner, after using 843 + 859 in the Live tab: the floating panel covered the picture. The freeze has a **VIC view** toggle; on, a translucent grid lies over the whole picture — raster line × cycle, each cell coloured by what the VIC and CPU did (BA, halt, c-/s-access), every store to the VIC marked where it lands. **Objects** frames what is on the screen — display objects as connected non-empty cells sharing mode (per-cell MC bit included), screen, charset/bitmap and bank, so an MC-char logo in hires text is its own object; sprites per appearance — each with the ranges its bytes live in. **Techniques** are rules over the record (split, FLI, FLD, linecrunch, DMA delay, open borders, multiplexer, crunch/stretch, mid-line), as the owner asked: a deterministic basis. Inspector in the right column, 859's strip in a dock. `vic/frame_map`, `runtime_vic_frame_map`. Gates: `vic_line_trace_gate` 12/0, `smoke:860` 14/0, `e2e:843-inspect` 40/0. | 2026-09-19 |
| 863 | NTSC (`../TRX64/docs/_archive/863-ntsc.md`) | **BUILT 2026-09-19 — merged 2026-09-20, both repos** | The owner: "NTSC Mode support in TRX64 und ein Switch im C64RE dafür." TRX64: a C64 model is a row of a config file (VICE's model table; a row naming a missing block is refused by name — after 863 `c64-pal`, `c64-ntsc`, `c64-paln` run), the VICE NTSC cycle tables 1:1, one timing record for every frame/clock consumer, the wrapped NTSC display window, the standard recorded in and enforced on every snapshot. PAL stays bit-identical; the palette stays Colodore. **C64RE half, as built:** C1 — the Live tab's model selector beside the power button, filled from `session/models` (unrunnable rows disabled, naming the missing block), asks before it switches (next frame; the program keeps its state and the standard it detected at boot; power-cycle for a clean start), then `session/model`; cancel sends nothing. `model` on `runtime_session_start` (replaces `pal`; another model switches the shared machine at the frame boundary and the tool says so), `runtime_sandbox_run` and `runtime_scene_reel`; `knowledge/project.json → machine.model` (`project_init` stamps `c64-pal` on a new project, `machine_model` sets it) passed as `--model` by the one spawn resolver — workspace, MCP auto-start, sandboxes. C2 — `PAL_CYCLES_PER_FRAME` is gone: every frame count reads the machine (`hold_frames`, `runtime_type` settle, the reel and sandbox runners, `I wait N frames` keeps its unit, the SID note names); the Export tab's seconds use the clock of the model each scenario was RECORDED on, which `runtime/scenario_list` now names (`model`, `null` for a scenario that names none) — only a runtime without the field falls back to the running machine's clock, and the tab says so; recorded scenarios carry `# model:` (the model the recording started on) and a run on another model is refused naming both; a switch while recording is a step, `the machine switches to <row>`, written where the runtime's journal recorded it (the wait before it ends inside the frame the switch closes, clear of its first raster lines — half a frame before it, the last part in cycles, where rounding down would land on a wrap — so the replay switches on the same boundary; a press held across it is written `I start holding …` … `I release …`, never split), executed through `session/model` by the reel and sandbox runners, the frames after it counted in the new model's frames — REC no longer warns about it; tool descriptions give both standards. C3 — the VIC view and the line strip take cycles, lines, origin and blanking from the recorder header (`frame-geometry.ts`), NTSC's wrapped window drawn where the recorder puts it. Gates: `e2e:863-model` 61/61 (hermetic, in CI), `smoke:863` 50/50 against the TRX64 branch, `smoke:814` 82/82. **Recorder timing, same branch:** the wait after `I hold … for N frames` is the gap minus the hold, every wait is written from where the replay is (rounding never adds up), a press something happens inside is written `I start holding …` … `I release …` (a start without its release is a parse error), both runners advance to an absolute cycle so a bounded run's overshoot never accumulates, and `Given the snapshot` undumps — `smoke:814` records a hold, a key after it and a key inside a held stick on PAL, switches to NTSC, does the same, and replays it with every journal entry on its recorded cycle. Both halves merged together on 2026-09-20 — this C64RE reads identity fields a pre-863 runtime does not report and refuses rather than assume PAL. | 2026-09-19 |
| 861 | [The cost of a change](_archive/861-the-cost-of-a-change.md) | **BUILT 2026-09-20** | Three instruments over one question. `change_impact` walks the graph upstream by depth — callers, the readers of what a range writes, the pointer tables that point INTO it, the documents and findings whose claims may be false afterwards — with UNKNOWN as its own class (an indirect jump, a computed return, a write into the code, the drive's CPU) and never folded into low; what the change must preserve composes 826's signature with §3.3's liveness. `code_cost` prices bytes exactly and cycles as a span whose only width is the page crossings and the branches, resolves a constant-bound loop to the cycle (567 for `ldx #$27 … dex / bpl`), and decides EQUIVALENT / NOT EQUIVALENT / UNKNOWN by executing two straight-line versions symbolically — registers, stack, every memory cell, the live flags, and every access to $D000-$DFFF in the same order, because reading $DC0D changes the machine. `trace_cost` evaluates what the runtime already records, or records it itself on a machine of its own: measured cycles from Δclock, the table's answer with the page crossing and the branch resolved from the trace, an interrupt dispatch's 7 cycles charged to the entry, and the remainder — the cycles the VIC stole — on the raster line they were stolen on. **TRX64 unchanged.** Two gaps were closed in C64RE's own graph instead: 819 records the exits no edge can carry, and the resolver stopped dropping a pointer table's xrefs. Gates: `check:cycle-table`, `e2e:861-static` 33/33 and `e2e:861-impact` 20/20 hermetic and in CI; `smoke:861` 40/40 against the real runtime — the cycle table proved opcode by opcode (244 of them, 137 591 cycles measured against 137 591 from the table), the stolen cycles matched against 859 for the same frame (1075 against 1075), the interrupts recognised from the traced vector reads, and a `sta $D020` on line 100 cycle 13 in both records. **One thing could not be derived and was reported rather than worked around (§5):** the drive lane is a deduplicated PC sample with no opcode, so §7.8 is refused with the smallest TRX64 change named — one row per retired drive instruction with its opcode. | 2026-09-20 |
| 862 | [Finding optimisation candidates](_archive/862-finding-optimisation-candidates.md) | **BUILT 2026-09-20** | The half 861 left out, built on it. Finding is a heuristic, the verdict is not: eleven rules in one table propose a rewrite, and every proposal is judged before it is shown — by 861's symbolic equivalence where both versions are straight-line code, by a CHECKED control-flow identity where they are not (`jsr x / rts` against `jmp x` differs only in how deep the stack is while x runs, so the callee's own bytes are read for a `tsx`, a `txs`, an `rti` or an unbalanced pull), and by an exact count for the structural class, which says it is a measurement. NOT EQUIVALENT ends a candidate and is counted per rule; UNKNOWN is shown and marked. The dataflow rules needed 861 to take an entry seed — the facts are derived by running the block's prefix symbolically, never pattern-matched, and a `jsr` in the prefix yields none. Code whose purpose IS its timing is refused and listed with the reason: a delay loop that only counts, a raster wait, a video-register write the capture puts inside the displayed picture, the 1541's VIAs, a byte-ready `bvc`, a fastloader's serial loop, self-modified bytes, an indirect jump, a stack trick. `zp-promote` takes its destination from Spec 844's free-RAM slot and inherits whether a run confirmed it; with no slot there is no candidate, and the rule says so. Undocumented opcodes are a class of their own and off by default. **Nothing is applied** — the graph opens read-only and the gate proves it by the empty write-ahead log. §4's `tail-call` row says 6 cycles and the answer is 9; there is no `sax` rule, because the sequence it would replace needs a scratch cell and is therefore not equivalent. Gates: `e2e:862-rules` 75/75 and `e2e:862-rank` 23/23 hermetic and in CI, `smoke:862` 19/19 on a real capture — 13 656 cycles of page crossing measured, 13 656 reported, and 2 cycles in a routine that ran 5690 times outranking 9 in one that ran once. **TRX64 unchanged.** | 2026-09-20 |
| 865 | [Disassemble bytes at an address](_archive/865-disassemble-bytes-at-an-address.md) | **BUILT 2026-09-20** | Every door into a listing wanted a PRG. What a session holds is a depacked chunk, a relocated overlay, a block out of a raw track: bytes and the address they run at — and on one day two autonomous runs closed that gap by hand, one writing its own disassembler and calling it 180 times, the other bolting fake 2-byte headers onto 308 blocks "so the MCP disassembler can eat them". Both then held listings the project knew nothing about. `disasm_raw` is the door, and the proof that it is not a second disassembler is the diff: the renderer's entry point gained ONE branch — `readRawImage` beside `readPrg`, both yielding the `{loadAddress, data}` pair the decoder always wanted — and everything past it is the same decoder, the same segment-aware path, the same annotation handling, the same `.asm`/`.tas` pair. Nothing is prepended to the bytes and nothing on disk is rewritten. It reassembles against the input range, header-free on both sides, and reports byte-identical or names why not; the listing carries its own provenance in its head and the artifact row carries it in prose, so a listing found months later can say which file, which byte range, which address; a re-run with the same arguments updates that row instead of minting a second, and where the bytes are a payload the link goes through the shared predicate, so one `extract_disk` made is not invisible to it. **Two fixes landed in the shared code and `disasm_prg` has both**: the linear renderer now honours the entry points it had been accepting and silently dropping — a seed inside a decoded instruction breaks it, the bytes before it become data, byte-exact either way — which in turn retired the pipeline's guessed `[0x0827]` default; and an annotations file can now be named outright, so the same file through both doors yields the same listing line for line. **One acceptance item turned out to describe a failure this renderer does not have** and is recorded rather than quietly weakened: a data-filled range does NOT diverge (unknown opcodes render as `.byte`, operand widths are forced, and 256 bytes of pseudo-random data rebuilt identically), so the negative fixture is a relative branch whose target wrapped below $0000, which the assembler refuses by name. Gate: `e2e:865`, hermetic and in CI — 55/55 with KickAssembler, 47 pass and 4 loud skips without. **TRX64 unchanged.** | 2026-09-20 |
| 866 | [Two doors, not four](_archive/866-two-doors-not-four.md) | **BUILT 2026-09-21** | Four doors answered two questions: `disasm_prg` and `disasm_raw` ran the same decoder and differed only in whether two bytes at the front are a load address, and headerless bytes could not be analysed at all — so a run over four G64 sides built fake load headers for 1541 drive code and wrote `.prg` copies. Two doors now, and one rule: **the load address decides how bytes are read, never the file name**. Given, they are raw and start there; omitted, the file must carry a header; both and disagreeing is refused naming both, and the answer always states which reading it took. `analyze` runs the nine analysers on either. Not a second decoder — one body, one renderer, and what it needed went into the shared code once. A render's analysis is: the one you named if it exists, else the one the **project store** has registered for these bytes, else the file beside them — which is the half of the defect that had a session re-running the analyser for every extracted file. The three old names are aliases of the two for one release, each naming its successor once. `e2e:866` 65/0 (CI, hermetic). TRX64 unchanged. | 2026-09-21 |
| 867 | [The window a payload owns](_archive/867-the-window-a-payload-owns.md) | **BUILT 2026-09-21** | Identity already carried the owner, so `$7400` in two modules was already two nodes; what was missing was the layer above — the WINDOW a payload occupies while it is loaded. Without it the graph's code seeds were filtered by owner alone, so each of six modules sharing `$1000` had its OWN entry points refused as somebody else's, and a payload spanning `$0A5F-$CBDA` inherited the engine's routines merely for overlapping them. The model is two rules: payloads that load at the same address are occupants of ONE window, never each other's holes; a window with a different start lying wholly inside another is a hole punched in it, and at an address it covers the outer payload is not the claimant. Seeding is scoped to the window, and an address the window puts outside this payload's business is **out of scope, not refused** — nobody asked for it, so nothing was taken away; 838's `owned_by_other` subtraction survives only where the other owner has no window at all. Measured on the reported project, same graph both ways: the 50 KB file went from 40 cross-owner seeds and 36 refusals to **1 seed, 75 out of scope, 0 refusals**, while the 43 addresses its own owner claims still seed; a module went from 3 seeds and 4 refusals to 6 and none. The payload record gains the window, the doors write it, and **nothing is migrated** — an older record's window is derived on read from the load address and the extent, and `list_payloads` says when it was. An address with no payload names its claimants and picks none (`graph_find`, `graph_node`); which claimant is IN the window comes from 804's byte-match where a capture exists, else from the load call that put it there (750.4's sector-load shape, extended to carry every position it is called with), else it is a named ambiguity. **One deviation, recorded:** an image's own window never comes from the graph's extent — that is a lower bound and would drop addresses that are in the image — it comes from the payload record or the range being analysed. `e2e:867-window` 51/0 (hermetic, in CI), `smoke:867` 10/0 against a real machine. **TRX64 unchanged.** | 2026-09-21 |

| 817 | [One platform knowledge base](_archive/817-one-platform-knowledge-base.md) | **BUILT 2026-09-05** | "What is at $D018" was answered in FOUR hand-typed tables that disagreed — 16 addresses defined twice, 8 with different names, $D011 and $D016 sharing one string. Structural, not sloppy: two `platform-knowledge` directories because ESM `src/` and CommonJS `pipeline/src/` cannot share a module, the C64 table dropped in the copy, the pipeline gateway returning a literal `C64_EMPTY`, and comments on both sides waiting on a rewiring that never came. **A store fixes what a tidy-up cannot: a SQLite file is readable from both halves.** Built: `resources/platform-kb.sqlite` (node:sqlite, 8 188 nodes, 153 regions, 1.9 MB) seeded from the c64ref snapshot — extended by two parsers so ZP, the RAM map and every I/O register come from the same upstream as the ROM (it had covered $A000-$FFFE only) — plus the ONE repo-owned extension table (EasyFlash, 1541). Node id DERIVED from `(platform, kind, address)`, re-seed content-identical. The four tables and both directories are DELETED; renderer, `inspect_address_range` and the MCP read the store through two 60-line readers. Gate `npm run check:platform-kb`: no address→name table outside the seeder, re-seed identical, $D011=SCROY≠$D016=SCROLX, $FFD2=CHROUT, kernal-abi names agree with the store, fixture renders from the store and rebuilds byte-identical, open+lookup 0.27 ms. The gate found a FIFTH table on its first run (`inspect-range.ts`, 29 VIC names) — which is the argument for the gate. First vertical slice of the semantic-knowledge-graph roadmap 818–824. | 2026-09-05 |

| 818 | [Address identity, banking, and the minimal query API](_archive/818-address-identity-and-query-api.md) | **BUILT 2026-09-05** | The one id grammar every graph slice keys on, and it is DERIVED, never assigned: `platform:kind:addr` for the platform (`c64:io:d018`), `slug:ctx:kind:addr` for a project (`wasteland:ram/block2_engine_0200:routine:1dd2`), the ctx naming WHERE THE BYTES ARE (`ram/<owner>`, `crt/<bank>`, `drv/<owner>`) — banking is identity from the first slice, not a later enrichment, because keying on address alone would have changed every id when banking arrived. Instructions are evidence on edges, not nodes: an instruction has no identity that survives relocation, depacking or self-modification; a routine and its target do. `layer` is in the primary key so one id has one generated and one human row, generated is replaceable, human untouchable. Built: `src/knowledge-graph/` (ids, schema, store, query, CLI), `<project>/knowledge/graph.sqlite` (node:sqlite, WAL), `c64re graph find|callers|callees|readers|writers|references|path|dump|stats`. The gate found the first defect: the replacement unit is the producing RUN's owner, a column of its own, because `addr` and `crt` nodes carry no owner in the grammar. Gate `npm run e2e:818` 24/0: refusals by rule, idempotent canonical dump, human orphan kept and flagged, platform resolution + dangling, empty stderr, 0.6 ms. | 2026-09-05 |
| 819 | [Symbol and control-flow graph](_archive/819-symbol-and-control-flow-graph.md) | **BUILT 2026-09-05** | The first producer of project nodes, from the EXISTING discovery output — no new disassembler. Routines (entry ∪ jsr target ∪ block-graph root), labels (jmp/branch targets), extents over `basicBlocks.successors`, and CALLS / CALLS_ROM / JUMPS_TO / BRANCHES_TO / CONTAINS edges, each carrying the instruction as evidence. Typed by the INSTRUCTION, not the xref's `type`: discovery tags every non-jsr/jmp absolute operand `branch`, so a `sta $D011` was a branch on paper — 947 of 1 794 `branch` xrefs in the corpus are memory operands; the producer emits nothing for them and 820 takes them. A jsr target that is both a platform ROM node and an in-image instruction (lnr_boot carries a RAM copy of the KERNAL) gets BOTH edges, `inferred`, ambiguity named. Runs from `importAnalysisArtifact` (soft-fail, additive) and `c64re graph seed`. Gate `npm run e2e:819` 15/0: pipeline-analyzed fixture with ground truth, zero false BRANCHES_TO over 1 059 branches in 21 real reports, lnr_boot 84 ms, `callers(c64:rom:ffd2)` = 6 with instruction and ambiguity, idempotent, human name survives. | 2026-09-05 |
| 820 | [Memory access graph](_archive/820-memory-access-graph.md) | **BUILT 2026-09-06** | READS / WRITES / *_INDIRECT / USES_ZP / USES_HARDWARE / REFERENCES_DATA from the existing discovery output. Measured first: `ReferenceType` declares read/write but NO producer emits them; every data reference lives on `codeAnalysis.instructions`. The target of an indirect access is unknown, and unknown is a value: the edge points at the POINTER (`c64:zp:0020`, 818's `to_id NOT NULL`), evidence says `target: unknown`; a same-routine construction with a constant target ADDS a second edge, never replaces the first. Two input defects found by the fixture: discovery flags `lda $D011` as control flow (the classifier now ignores the flag), and `$20` was landing on a project address because the platform store does not document it — zero page, I/O and ROM now derive their platform id from the address alone. Counts reconcile on lnr_boot exactly: 2 323 = 1 008 + 965 + 2×175 direct rows, 204 indirect, every one `unknown`. Corpus: 3 110 direct, 211 indirect, and **1 resolved (0.5 %)** — the pointer bytes are loaded dynamically; that number is 821's job. Gate `e2e:820` 21/0, `measure:820`. 820.2 (D7) built: the four private walks — `inspect_address_range`'s xrefs, `ram_report`'s access table, `address-index`'s xref index, the evidence graph's reads_from/writes_to — read the store; output parity on lnr_boot byte-identical except listed additions (the mistyped `branch` xrefs become read/write, −3 435 fallthrough adjacencies, −327 zero-touch rows the old walk invented). Loud JSON fallback when the graph is absent; the MCP side seeds on demand. The parity gate found a 819 defect: every jsr's `fallthrough` xref had become a CALLS to pc+3 (330 phantom calls on lnr_boot) — fixed. Gate `e2e:820-2` 40/0. Still open: `jmp (abs)` vector reads are recorded by nobody. | 2026-09-06 |
| 821 | [Runtime graph enrichment](_archive/821-runtime-graph-enrichment.md) | **BUILT 2026-09-06** | Same edge types, `origin=runtime` — no `*_RUNTIME` family; the column is the filter. An observation CONFIRMS by adding a row (`observed`, with pc, effective address, run, cycles, count, old_value, flow main|irq|nmi), never by promoting a static edge. The importer reads the `.c64retrace` binary (726.B: the log is the authority), never the DuckDB, so import is daemon-free; the gate passes with the daemon port closed. Built against 818 as built: no schema columns, evidence JSON, identity `run:<id>:pc:<hex4>`, replacement unit = the run. A run is a node (`slug:sub:run-<id>`, the grammar's address-free form); `EXECUTES`, `HANDLES_IRQ` / `HANDLES_NMI` from the run node. Found reading TRX64: the bus record's `pc` is the live post-fetch PC, not the instruction address — the importer attributes accesses to the retiring CPU_STEP. Targets follow the reconstructed `$01` lens (ROM only while mapped; RAM-under-ROM → addr node). Gates `e2e:821` 48/0 (synthetic capture) and `e2e:821-real` 8/0 on two local captures (42 MB in 436 ms, 266 MB in 1.8 s; skips loudly without one). CLI: `import-trace`, `runs`, `observations`, `pointer-targets`, `unconfirmed`, `unexplained`, `irq-handlers`. Not built: an MCP `graph_import_trace` door — 823 is five tools by design; importing a trace is a `c64re graph import-trace` step for now. | 2026-09-06 |
| 822 | [Human knowledge integration + migration](_archive/822-human-knowledge-and-migration.md) | **BUILT 2026-09-06** (822.1 + 822.2) | The cut-over spec. 822.1 built: the migration by derived id (Wasteland_EF, 46 936 legacy records in 942 ms: 22 246 analysis entities → 4 612 nodes, 9 421 findings → 1 189 claims + 9 421 evidence rows, `$0031` = one claim with 58 evidence rows, 3 640 heuristic questions → 0 rows), the human door (`nameNode`, `annotate`, `linkNodes`, `assignSubsystem`, `answerQuestion`), annotations with FTS5, the conflict rule as 818's primary key, a ledger that makes re-runs idempotent AND incremental. Measured, not estimated: the spec's collapse numbers were high (6 605 → 4 612) because addressless and prose-only records fold rather than become nodes. 2×200 concurrent door writes: 400/400; the JSON path: 200/400, one writer dies on the rename. Gate `e2e:822` 90/0 on a tmp copy of Wasteland_EF (skips loudly without it); human-layer hash invariant across an 819 re-seed. 822.2 (the cut, same day): the graph IS the store for findings / entities / relations / open questions / user labels — `records.ts` projects it into the record shapes, every `save_*` goes through the doors or the importer, `project_search` and the views read the graph, `emitAnnotationFindings` + `import_annotations_as_findings` deleted (the annotations file is a door: `disasm_prg` imports it on change), projects cut over on open (files → `knowledge/_legacy-822/`, timeline note), `c64re graph export`. No dual-write window (§6, §11). Gates: `e2e:822` 111/0, `check:822-no-json-readers` GREEN; 400 service writes 400/400 in 9.8 s where the JSON path lost 200. Two 818/822.1 defects fixed on the way (nested attrs dropped by the sorted-keys replacer; door rows keeping producer 822). | 2026-09-06 |
| 823 | [MCP surface over the graph query API](_archive/823-graph-mcp-surface.md) | **BUILT 2026-09-06** | Five thin tools — `graph_find`, `graph_node`, `graph_edges`, `graph_path`, `graph_overview` — where the draft had twelve; a handler is parse → one library call → format, and the gate reads the file to keep it that way. ONE formatter for CLI and MCP: `c64re graph find --json` equals the tool's JSON block byte for byte, asserted. Compact hits with stable ids that round-trip. Default surface, cap 151 → 156 with the reason written down (the cap was full; OQ1 names the demotion candidate). 740.1 coexists with the boundary in both descriptions: `project_search` = where it is described, `graph_*` = what the machine does. The three doctrine-text changes (agent-doctrine §1, server instructions under STATIC-FIRST, a playbook step) landed in the same commit. Gate `e2e:823` 28/0 through the real server over stdio on a project created by `project_init`; the first run failed on every call because the server refuses an uninitialized directory — the gate now goes through the real door. Pre-existing probe reds (validate_extraction, runtime_candidate_*, sandbox_*) were red on master and name no graph tool. | 2026-09-06 |
| 824 | [Graph UI](_archive/824-graph-ui.md) | **BUILT 2026-09-06** | Routes first: five `GET /api/graph/*`, one per 823 tool, the body IS the tool's JSON block (823's formatter once more; the smoke asserts byte-identity with `c64re graph find --json`), 404 with `next[]` when no graph exists yet. Then ONE shell: a `graph` tab next to Flow Graph, never a second app (`/v3.html` stays 404). A NEIGHBOURHOOD, not the graph: one focus node, incoming lane left, outgoing lane right, in FlowPanel's own SVG vocabulary, filter chips that dim rather than remove, depth 1–2, dangling targets dashed. No new dependency — the smoke freezes the list. Click → the existing `handleSelectEntity(…, "listing")` plus the `scrollIntoView` the listing lacked. Gates `smoke:824-routes` 11/0 (real workspace server on a temp project), `smoke:824` 14/0 (bundle + source). 824.2 built: a pure address→line mapper (`ui/src/lib/asm-address-map.ts`) anchored on what the renderer emits — `.pc =`/`* =`, `.pseudopc`/`.logical`, the SEGMENT header, `WXXXX:` labels — counting instruction bytes forward and re-syncing at every anchor; zero drift on a 795-line listing with 94 labels. AsmView takes `jumpToAddress`; the Graph card gains "Open in source". Gate `smoke:824-2` 25/0. Pre-existing and recorded: 15 `ui:typecheck` errors in App.tsx/workspace-panels.tsx, 15 before and after. | 2026-09-06 |
| 825 | [Graph explorer: sigma.js, four projections](_archive/825-graph-explorer-sigma.md) | **BUILT 2026-09-06** (master) | The whole-project explorer 824 D3 named and declined a library for. Measured first: 824's lane SVG is unreadable on the first real routine (`fastload_main`: 1 caller, 80 callees, text under 4 px), Wasteland_EF is 11 005 nodes / 21 910 edges with ZERO subsystem nodes, and GitNexus's three views are one in-memory graph (sigma.js 3 on graphology, 24 k / 43 k nodes, 2.9 MB) under three layout functions. Decisions: ONE bulk route `GET /api/graph/subgraph` + `c64re graph subgraph --json` (byte-identical, collapsed edges with `n`, platform endpoints synthesised, sorted ids; no sixth MCP tool); sigma.js + graphology pinned and lazily chunked (revises 824 D3, the frozen-list tripwire grows by exactly these names); FOUR projections as pure layout functions over one graphology model — Force (ForceAtlas2 worker, seeded), Layers (entry → routine → data → addr → zp/io/rom), Radial (rings by hop from the focus; depth 1 IS 824's neighbourhood, the lanes retire), and the C64-native Address view (x = $0000–$FFFF, a lane per bank — 818's banking-in-identity pays off); communities human-first (`assign-subsystem`), Louvain over CODE edges only for the rest, never written back; filters are lenses that never move a position; the inspector docks (`AsmView` + `jumpToAddress` beside the canvas). Gates: `smoke:825-routes` (route = CLI byte for byte, Σn reconciles with the store), `smoke:825` (lazy chunk, pinned list, pure layouts unit-tested, Louvain ignores memory hubs, filters leave positions untouched), `measure:825` on Wasteland. Built: gates 32/0 and 59/0, 824's three and 823 still green. Measured 13 060 nodes / 28 761 collapsed edges in 95-206 ms, 162 communities at modularity 0.845, layouts under 10 ms each. D7's byte target missed and left missed — 11.92 MB raw (374 kB gzipped) because 823 D7 makes the body byte-identical to the CLI's pretty-printed JSON; compacting it would move every route at once. Four things the spec did not know: sigma reserves the edge attribute `type` for its render program (store types moved to `edgeType`), its camera is y-up so the flip lives at the render boundary and not in the pure layouts, Louvain over code edges leaves most nodes without one (grey, not 8 028 singleton communities), and the Graph tab needed the single-column treatment or the canvas was 411 px wide. §10 (the first look on screen): the palette was an `hsl()` string sigma parses to BLACK, so 13 060 nodes drew black on a near-black panel; labels were force-drawn on every platform node and each carries a c64ref heading; 6 s of ForceAtlas2 on 13 060 nodes leaves the circular seed a circle; and a fixed 4096-per-lane Address stack makes sixty EasyFlash banks four times taller than the address axis is wide. All five fixed and gated — `smoke:825` 85/0, and it now hands every colour to sigma's own parser. | 2026-09-06 |
| 826 | [Routine signatures](_archive/826-routine-signatures.md) | **BUILT 2026-09-06** | WL1's first field test answered "how does the game find payloads" from the graph alone and got the ABI wrong: Mode/Track/Block travel in A/X/Y, `$FC` is the return status, the mode byte `$F3` (01/02/03) was invisible — the graph had no notion of input or output. The 6502 declares no interface, but it is COMPUTABLE: liveness on 819's block graph gives every routine `in` (read before written), `out` (written on a path to `rts`), `clobbers`, `preserves`, over registers, flags (C/V like registers, N/Z only when branched on), zero page, stack slots and PATCHED OPERANDS (`sta $2801` = a parameter of the routine at $2800). Interprocedural bottom-up over CALLS (SCCs), a `jsr $FFD2` contributes the KERNAL's summary from a new `platform_abi` table in the ONE store (schema 2), an unknown callee makes the summary `partial` and says where. Arguments at the call site: a backward slice from the `jsr` to the last write of each live-in — `A=#$01`, `X←$27E1`, `Y←op:$2805` — so across all callers the immediates form the DOMAIN of a parameter (`A ∈ {01,02,03}`: the machine finds THAT there is a mode byte and WHICH values; the human names what 02 means, D8). The stack is tracked per block: `pha pha rts` dispatch → JUMPS_TO, `pla pla` → returns to the caller's caller, `tsx; lda $0101,x` → inline arguments. 821 records the observed A/X/Y at every retiring `jsr` beside the static slice. 826.0 built first — the six fidelity fixes WL1 measured: T1 CALLS_ROM only where the ROM can be visible (306 of 328 were game code under BASIC), T2 `RESOLVES_TO` across artifact owners (graph_path was trapped in one artifact), T3/T4 human nodes without a twin by kind (label → containing routine, segment → data_block, routine inside/outside → boundary report of 819's 28 disagreements), T5 `c64:zp:00fe` resolves by grammar (no book line needed), T6 `find` reads the annotations' FTS, T7 an artifact needs a MACHINE (`graph machine <owner> c1541` — drive code had been seeded as C64 RAM and looked plausible). Built the same day: gates `e2e:826` 51/0, `e2e:826-boundaries` 40/0, `e2e:826-runtime-args` 44/0; Wasteland copy seeds in 1.3 s, 939 routines, 368 signed / 571 partial, `args $FC00` = 7 sites with the mode byte and the patched operands $2801/$2803/$2805. | 2026-09-06 |

**READY** = the next step is written down; someone could start tomorrow.
**NEEDS SCOPING** = something is open but nobody has said what, so the first task is to
decide. **BLOCKED** = waiting on something outside the repo.

There is no "in progress": nothing here is being worked on, and a status that claims
otherwise is a lie the folder tells every visitor. The date column is the honest version.

Where a spec shipped and only a named part is left, that is said in *What is left* — it
is a fact about the past, not a status. Read those files knowing most of what they
describe already exists, and that changing it risks working code.

---

Every spec above is **C64RE workbench**. Everything built through July and August was
**TRX64**: runtime, trace, snapshots, sandbox, container, release, monitor. That is a
priority decision, written here so these stop reading as imminent.

TRX64's own specs live in `../../TRX64/docs/`, on their own board at
[`../../TRX64/docs/README.md`](../../TRX64/docs/README.md) (added 2026-08-12, with
`scripts/check-spec-board.sh` keeping the board and the specs from disagreeing — four
were stale when it was written). That board is TRX64's STATUS; THIS file stays the one
number registry, and the next free number comes from here. Only
those with C64RE-side work appear here (803 has none left).
