# Archive

Specs that are finished, decided against, or retired. **Nothing here is open work** —
`../README.md` holds that, and it holds only that.

Kept rather than deleted for two reasons. A **WON'T-DO** is a decision: without the
record, the same idea gets proposed again in a month. And a retired rule explains code
that still exists — delete the rule and the code looks arbitrary.

Verdicts: **DONE** shipped · **WON'T-DO** decided against, with the reason · **RETIRED**
was binding doctrine, no longer is (the surviving rules live in `../../DOCTRINE.md`).

## Recorded

The 56 specs below carry the decision that closed them.

| # | Verdict | Spec | Decision / what shipped |
|---|---|---|---|
| 422 | **WON'T-DO** | [IEC Burst mode](422-iec-phase-g-burst-mode.md) | **dead** — JiffyDOS/burst; no game in scope needs it. Rebuild on demand or accept an external MR. |
| 424 | **WON'T-DO** | [Drive + Cartridge LED + Inspector UX](424-drive-cart-led-and-inspector-ux.md) | Closed 2026-08-11, both axes obsolete. **A (LED/status):** targeted the TypeScript runtime's `session/drive_status` and the vice1541 probe — the deprecated oracle, not the product. TRX64 owns the runtime and surfaces the drive LED from VIA2 PB3 directly. The four approximations still in the TS handler (no PWM curve, `ledFlashing` hardcoded false, motor inferred from the LED, R/W fixed to read) are noted in the code and stay as oracle-only gaps. **B (Inspector UX):** described moving a media strip into a right inspector column of the v3 shell — a layout the workbench no longer has. |
| 428 | **WON'T-DO** | [Split C64 + 1541 CPU contracts](428-split-c64-and-1541-cpu-contracts.md) | **dead** — TS CPU; settled by Spec 723 single-path. |
| 610 | **RETIRED** | [1541 Parity Rebuild Charter](610-1541-parity-rebuild-charter.md) | dormant (TS oracle only) |
| 612 | **RETIRED** | [1541 Port Fidelity Rules + TODO](612-1541-port-fidelity-rules.md) | retired as a mandate 2026-07-15; CI gate `check:1541-fidelity` still runs |
| 613 | **WON'T-DO** | [c64 IEC `LOAD"$",8` regression](613-c64-iec-load-regression.md) | **dead** — TS drive; downstream KERNAL-load fidelity long landed. |
| 614 | **WON'T-DO** | [Drive per-cycle scheduling](614-drive-per-cycle-scheduling.md) | **dead** — TS drive; vice1541 bridge + 622 §4.0 shipped. |
| 615 | **WON'T-DO** | [GCR decode fidelity](615-gcr-decode-fidelity.md) | **dead** — TS drive; 616/617 byte-fidelity DONE + post-mortem recorded. |
| 619 | **WON'T-DO** | [VICE / Headless KPI Trace Contract](619-vice-headless-kpi-trace-contract.md) | **dead** — TS-trace KPI; absorbed by the shipped trace stack / TRX64. |
| 620 | **RETIRED** | [Port-Bug Forensic Doctrine](620-port-bug-forensic-doctrine.md) | retired 2026-07-15; its technique = `CLAUDE.md` rule 5 |
| 621 | **WON'T-DO** | [1541 Port Hygiene Enforcement Backlog](621-port-hygiene-backlog.md) | **dead** — TS `vice1541/**` cleanup; no more TS-drive work. |
| 622 | **WON'T-DO** | [vice-mode Headless Performance](622-vice-mode-performance.md) | §4.0 shipped and measured (`2d9e4de`): 0.50× → 0.82× realtime. Closed 2026-08-11 — its premise and a third of its work no longer exist. The `drive1541Implementation="vice"` mode the problem statement compares against was removed by 723 (VICE1541 is the only drive). §4.3's suspected double dispatch is between `cycle-lockstep-scheduler` and `cycle-wrappers`, and 723 deleted the former. §4.1/§4.2 are unscoped V8 micro-optimisation candidates from a May profile, against the TypeScript runtime — the parity oracle since 2026-07-15, not the product. An oracle is run to compare, not to play; TRX64 is the product runtime. |
| 623 | **WON'T-DO** | [VICE-compat monitor / debugger](623-vice-monitor-debugger.md) | **→ TRX64 (already there)** — monitor + reverse-debug in TRX64 (`MONITOR.md`); C64RE-facing part via Spec 754 (archived) done. |
| 700 | **WON'T-DO** | [Runtime Optimization](700-runtime-optimization.md) | **dead** — TS perf, TS is fallback; TRX64 owns perf (~8–10× faster). |
| 703 | **DONE** | [SID reSID Audio](703-sid-resid-wasm-audio.md) | Live reSID audio + SID inspector shipped (`fb27a7d`). The one deferred slice, **703.5 WAV export**, was closed 2026-08-11 rather than built: the audio leaves the daemon as a stream, and capturing a stream to a file is what ffmpeg is for. Writing a second encoder inside the emulator buys nothing. |
| 704 | **DONE** | [Runtime Codebase Cleanup](704-runtime-codebase-cleanup.md) | §11 legacy-1541 retirement shipped (`0411295`). Closed 2026-08-11 with the other four phases resolved rather than built: **704.2** generated-output cleanup — `session/` and `snapshots/` are no longer in the tree at all; **704.7** spec hygiene — done wholesale on 2026-08-11 (this folder holds open work only, closed specs carry a decision here); **704.5** v3 transport and **704.6** SID cleanup both target the TypeScript runtime, the parity oracle since 2026-07-15, and `resid.ts` no longer exists — reSID is Rust in TRX64. |
| 705 | **WON'T-DO** | [Interactive Runtime Evidence / Intervention / Replay (contract)](705-interactive-runtime-evidence-intervention-replay-contract.md) | **→ TRX64** — the whole evidence/intervention/replay domain is TRX64-owned; children 711/712 folded below. |
| 711 | **WON'T-DO** | [Code/Data Overlay + Controlled Intervention Branches](711-code-overlay-intervention-branches.md) | **→ merged into TRX64** `docs/776-overlay-intervention-diff.md`. |
| 712 | **WON'T-DO** | [Rewind, Replay and Branch Diff](712-rewind-replay-branch-diff.md) | **→ merged into TRX64** `docs/776-overlay-intervention-diff.md` (rewind/snapshot-diff already in `spec-time-travel-tooling.md`; the new part = overlay-intervention + outcome-diff). |
| 713 | **WON'T-DO** | [VICE Cartridge Fidelity (CRT mapping/banking/writable)](713-vice-cartridge-fidelity.md) | **dropped** — TS-runtime cart-fidelity; TS deprecating + TRX64 already has faithful cart families (Normal/MagicDesk/Ocean read-only + flash-writable EasyFlash/GMOD/MegaCart, proven vs VICE). Branch `spec-713… |
| 715 | **RETIRED** | [Runtime Product Proof Baseline](715-runtime-product-proof-baseline.md) | retired as the authority → **783** |
| 721 | **DONE** | [Visual-Origin Join (runtime-informed annotation)](721-runtime-informed-annotation.md) | **DONE** — core join shipped, probe green. Provides the `mediumRef`/`MediaRegion` medium model + the trace→origin chain **Spec 750** consumes; the layout-placement slice 721.J5 shipped as **Spec 750.1**. The… |
| 723 | **RETIRED** | [Single-Path Runtime](723-single-path-runtime.md) | Closed for good 2026-08-12 by Spec 806: it governed the TypeScript runtime, and that runtime is deleted. Its gate `scripts/probe-single-path.mjs` went with it, GREEN 25/25 on its last run. The principle (a second execution path is still a second execution path) survives in TRX64; the full text and the reason are in `DOCTRINE.md` under *Retired 2026-08-12*. `CLAUDE.md` rule 1 is now "one runtime, and it is a separate process". |
| 726 | **DONE** | [Headless Trace Sink + Marks](726-mcp-headless-trace-sink.md) | DuckDB sink + marks shipped; **726.B slice 1 shipped 2026-05-30** — the binary `.c64retrace` log IS the timeline authority, as the spec itself records at line 25. The board carried it as open work for ten weeks anyway. TRX64 then built the native Rust reader for that format (802), so the remaining slice exists twice. Closed 2026-08-11. |
| 742 | **DONE** | [Media Ownership + VICE-Faithful Write-Through](742-media-ownership-write-through-refactor.md) | **DONE** — write-through (D64/G64 + EasyFlash CRT → host file) fixed + gated (BUG-023, `smoke:742` 9/9). The "7 divergent mount paths" concern is resolved by the single Runtime-Daemon API (744.4c: UI/MCP/CLI… |
| 744 | **WON'T-DO** | [Runtime Session Authority + Drive-to-State](744-runtime-session-authority-drive-to-state.md) | **→ TRX64 (already there)** — daemon authority + `media/*` (mount/swap) + drive write-back shipped; session-orchestration is normal daemon-client work. |
| 746 | **DONE** | [Live Trace + Scrub Workbench (charter)](746-live-trace-scrub-workbench-charter.md) | Charter, not a slice — the anchor + firehose model it defined is built. Trace core, checkpoints and rewind are TRX64-owned and shipped; the scrub UI it called for (§4.2 checkpoint/scrub tools) shipped under 765 and 769 (filmstrip scrub in `workspace-panels.tsx`). Closed 2026-08-11: a charter whose parts all landed elsewhere is not open work. |
| 747 | **WON'T-DO** | [Bun Runtime Investigation](747-bun-runtime-investigation.md) | **dead** — Bun host was for the TS runtime; Node stays baseline, TS deprecating. |
| 771 | **DONE** | [TRX64 Runtime Backend + VICE Deprecation](771-trx64-runtime-backend.md) | **DONE 2026-08-11** — the goal is the state of the world: TRX64 is the default backend (`resolveDaemonSpawn`; the TS runtime is reachable only via `C64RE_RUNTIME_TS=1` and explicitly never a silent fallback)… |
| 772 | **WON'T-DO** | [Checkpoint-Ring: Cadence + Retention](772-checkpoint-ring-retention.md) | **→ TRX64 (already there)** — checkpoint-ring done (TRX64 CHECKLIST 705.B); cadence is a config value, not a spec. |
| 773 | **DONE** | [Workflow Cockpit: the 5-phase lifecycle](773-workflow-cockpit-lifecycle.md) | The product-model correction landed: `src/agent-orchestrator/lifecycle.ts` carries the crosswalk (workflow phase → lifecycle, per-artifact phase → lifecycle, step → lifecycle), `App.tsx` has the phase strip as navigation rather than a gate, and 25 tabs are phase-tagged so the tool row follows the phase. Verified in use 2026-08-11: Onboarding · Discovery · Reverse Eng. · Build · Release down the side, and a per-phase overview of Known / Missing-blockers / Next action / Open questions. What the spec itself listed as deferred stays deferred and has no owner here: a dedicated decision record type, release tester-loop/RC types, a fused role/next-step field — and the Build patch-loop, which was waiting on 711 and is now answered by TRX64's 776/796/797. Further polish is a refactor, not this spec. |
| 787 | **ARCHIVED** | [**BUILT**](787-scoped-trx64-instances.md) | `787-scoped-trx64-instances.md` | **Scoped TRX64 instances** (foundation) — one live machine under the C64RE UI (shared-attach) + N throwaway **scratch** instances (sandbox/oracle/targeted runs). The "one ma… |
| 788 | **ARCHIVED** | [**BUILT**](788-real-core-execution-sandbox.md) | `788-real-core-execution-sandbox.md` | **Real-core execution sandbox** (consumer of 787) — retire the standalone TS `Cpu6502` (orphaned 3rd 6502: flat 64K, no IO/banking, refs the deleted `cpu6510.ts`); run … |
| 799 | **ARCHIVED** | [**BUILT**](799-trx64-docker.md) | `799-trx64-docker.md` | **TRX64 Docker image** (containerized emulator sidecar) — official OCI packaging of `trx64-daemon` (multi-stage Rust build, ROMs baked, one WS port, amd64 first) + a consumer contract… |
| 806 | **DONE** | [Retire the TS emulator and VICE](806-retire-ts-emulator-and-vice.md) | Written and shipped in one day, 2026-08-12, on branch `spec-806-structural-cut`. C64RE had carried two C64 implementations since TRX64 became the backend (771): the product runtime, and an in-process TypeScript emulator kept as a parity oracle. Keeping it had stopped being free — twice in two days a question about "the runtime" needed a detour to establish whether a file was the product or the oracle, and once it was answered from the wrong one. Deleted: 176 emulator files, 63 chip-level test files, 383 scripts, 22 VICE files, **49 `vice_*` MCP tools**, ~135k lines. Eleven ADVANCED-only tools retired with it, so the DEFAULT surface is byte-identical at 144 (total 302 → 253). Spec 723 (single-path) retired with its subject, GREEN 25/25 on its last run. The removal was **preceded by a structural cut**, and that was the part that mattered: 33 files under `runtime/headless/` were not the emulator at all — the trace reader that decodes TRX64 captures, the analysis layer, the recorder, the input config — and deleting the folder wholesale (which I tried first) produced 116 errors and a demand that the owner decide the fate of eight features. Measured properly, the split was computable and the tool cross-check against the runtime's 112 methods answered all but eleven. **Four path-breakage classes, every one silent:** relative specifiers escaping a moved tree, paths built from segments (`join(ROOT, "src", "runtime", …)`), roots computed by counting levels from `import.meta.url` (which stopped the 1541 ROM being found and pointed a package root above the repo), and whole directories outside `tsconfig` — `ui/`, `tests/`, then `scripts/`. A rename is only mechanical for paths written as paths. |
| 805 | **DONE** | [Sandbox batch: one process start for N runs](805-sandbox-batch.md) | Written and built the same day, 2026-08-11, off a measurement taken while sizing 774: `trx64cli` costs ~740 ms to start — ~650 ms of it eager machine init *before* argument parsing (a tiny binary from the same workspace starts in 86 ms; stripping made it worse, so not size). The C64RE depack bridge spawned a process per payload, so one proof project's 101 chunks meant ~75 seconds of pure startup for milliseconds of work. Two obvious fixes were both wrong: a scratch machine inside the daemon is two machines in one process (doctrine rule 2, and the module-global VIC/vice1541 state makes it unsafe, not merely disallowed), and re-implementing the run over individual RPC calls duplicates orchestration the CLI owns — seed/cart/disk, blob loads, ZP seeding, the `$01` config, an 11-byte entry stub the CLI builds, sentinel, caps, harvest ranges — and puts the two out of step the moment either changes. The cut was neither: one process start, N runs, the 787 scratch instance otherwise untouched. Each item runs on a fresh thread, not for parallelism (they are sequential) but because the core carries one thread-local — the IEC bus status arrays mirroring VICE's statics — that no machine constructor resets, so a batch item attaching a disk would have seen its predecessor's device flags. The bridge now builds a batch *spec* rather than an argv and the single-payload path is literally a batch of one, so the two cannot diverge. Measured: 20 runs 15.09 s → 0.83 s (18.1×) byte-identical; 10 payloads through the bridge cost 937 ms against 897 ms for one. Gate `e2e:805-sandbox-batch` 7/7. |
| 748 | **DONE** | [Project Steering + Agent Discipline](748-project-steering-and-agent-discipline.md) | Closed 2026-08-11. 748.1 (project `steering.md`, injected verbatim at the top of every `agent_onboard`) and 748.2 (the orchestrator teeth for BUG-032: heuristic questions behind a count, an ID-prefilled reconcile step when an open question overlaps an unlinked finding, and the discipline provisioned into every project) shipped — `e2e:748` 10/10. **748.3 was superseded by Spec 784 rather than delivered.** It wanted a tool that derives the cartography from a trace automatically; 784 built the correlation and the bulk registration, then decided against that direction: emulation is the validation oracle, never the default bulk path. The reason was measured, not preferred — attributing landed bytes to the sector under the head was the exact bug class the automation was meant to catch (Pawn: 78 landings on T35 that were the copy-loader relocating already-loaded bytes). Extraction now comes from a per-project extractor written off the loader disassembly, validated against drive-side truth. The one surviving piece, `loader-events` as records, was never this spec's subject and moved to 750. Also fixed here: the gate's assertion 5 asserted a prose string carrying a spec number that had been dropped from the steering heading, so it went red while the block was provisioned the whole time — it now keys on `RECONCILE_MARKER`. A gate that fails on a doc edit teaches people to ignore it. |
| 785 | **DONE** | [Cartridge extraction](785-crt-extraction.md) | Rewritten and built in one day, 2026-08-11, against two real cartridge projects. Its centre of gravity was not where the spec put it: the trace lane was the small part, and the real defect was that cartridge coverage collapsed byte-exact numbers into a per-chip boolean, so a project read "65/65 data blocks attributed" with **zero** payloads registered — 494 disassembly code islands happened to union-cover every chip. Registering 135 payloads moved that number by nothing, which is what made the case. Delivered: the registration bridge (135 and 554 payloads with full ordered slot spans through the same call path as a disk manifest), coverage in three axes in bytes (Data/Used/Identified, so "where is there room for a patch" is answerable), No Data scanned off the bytes, one cart view path, per-cartridge identity that exposed a project registering the wrong image twice, and `CART_READ (0x36)` in TRX64 — one record per bank residency, armed-only — which `validate_extraction` now diffs cart spans against instead of skipping them while its description claimed otherwise. Every read-set result is labelled by its run: the lane proves *used*, never *unused*. Three corrections the build forced back into the spec, each measured: a span derivation "verified" by reproducing 134 successor addresses was wrong by four bytes (successor arithmetic is invariant to a constant offset; only the byte comparison could catch it — 0/135 matched at `src`, 135/135 at `src+4`); `off_lo..off_hi` is a bounding hull, not a coverage set, and reading it as one produced a false refutation of a byte-verified manifest; and the two proof projects did **not** cover two index shapes — both are `structured`, the loading axis has exactly three values (`singleload` / `chainload` / `structured`), and `chainload`, the only one that needs a runtime, still has no example. Gates: `npm run e2e:785-cart-readset`, `e2e:785-identity`, `e2e:medium-coverage`. |
| 784 | **DONE** | [Loader-lens extraction](784-loader-lens-extraction.md) | Closed 2026-08-11 after checking every deliverable against the tree: B1 `loader-manifest.ts`, B2 `manifest-register.ts`, B3 LoaderModel records, B4 `validate-extraction.ts`, C1 `steering-defaults.ts`, A-side `loader-lens.ts` over TRX64's `BLOCK_READ (0x35)`. A1 landed as a **domain** (`drive-mechanism`) on the existing `runtime_trace_start` rather than the `loaderTrace` command the spec asked for — AC met, extra command unnecessary. It carried `PROPOSED (ready for build)` for weeks after shipping; Doctrine rule 9 exists because of this file. Its cart claims did **not** hold and are corrected in 785 §1. |
| 801 | **DONE** | [Artifact distribution](801-artifact-distribution.md) | TRX64-primary by design; the C64RE/npm half was explicitly deferred in §C.4. Delivered 2026-08-10/11 by other routes than the ones written down: ROM-less image, tag-driven publishing, per-platform binaries for five targets, a Homebrew tap that updates itself on release, and version discipline (one workspace version, image tag = binary `--version`). Of the three open questions, multi-arch is **built** (arm64 for Linux and Windows) and release notes are **hand-written per tag**. GHCR was NOT used — images build on the self-hosted runner and go to a private registry. The deferred C64RE/npm half moved to **716**, whose revisit condition ("someone needs to run C64RE without a checkout") is now met. |
| 804 | **DONE** | [Symbols are joined in C64RE](804-symbols-joined-in-c64re.md) | Built 2026-09-19 on branch `spec-804-symbols-in-c64re` (both repos; merged 2026-09-19). The owner reversed the old premise: TRX64 is a runtime and holds no symbols; C64RE owns meaning and TRX64 owns the monitor's command set. TRX64 lost its label layer and returns address spans + banking state on every `monitor/exec`; C64RE resolves names over user > build > derived, decides residency by comparing a payload's code bytes with memory (no match → no name, per trace row too), and substitutes/decorates with no verb list. Gates: TRX64 gate GREEN, `smoke:804` 42/0, `smoke:804-monitor` 25/0. Left: a "name this address" door; the wire epoch bump at release. |
| 843 | **DONE** | [Click a pixel, name the thing](843-click-a-pixel-name-the-thing.md) | Built 2026-09-11, merged and released with TRX64 0.8.0 on 2026-09-19. The root of seven Inspect defects was `vicProvenance` hardcoded to null at the capture site, so a raster-split screen resolved against the frozen registers; the per-line record is now written and restored. Pins, scrub, Promote and region drag fixed alongside. Gate: `e2e:843`. |
| 844 | **DONE** | [The questions are a schema](844-the-questions-are-a-schema.md) | "The questions are the same in every multi-disk project" made them a SCHEMA: fourteen slots, an empty required slot is a query result, REFUSE-or-REPORT decided per slot. Corpus law: a scan proves presence, never absence. Gates: `e2e:844-slots`, `e2e:844-teeth`. |
| 845 | **DONE** | [The model layer](845-the-model-layer.md) | A coarse layer above the graph (177 nodes where the graph has 11 161): boundaries asserted, membership computed by address containment, one re-entry read. A boundary is identified by its NAME, not its start address. `model_assert` / `model_read` / `model_remove`; gate `e2e:845-model`. |
| 846 | **DONE** | [The critic](846-the-critic.md) | Counter-pressure from the graph: a negative claim is hard to prove by scanning and easy to refute, so D1 is a negative-claim detector plus six graph-only checks; severity belongs to the CHECK; `readyToDesign` is computed and may say no; between refusing and driving lies ASKING. `project_critique` / `critic_checks`; gate `e2e:846-critic`. |
| 847 | **DONE** | [Documents declare themselves](847-documents-declare-themselves.md) | No new documents demanded: every synthesis document that exists declares in frontmatter what it covers, and that resolves in the graph; generated docs carry provenance so staleness is computable; the wiki index is derived. The logic lives in `c64re doc lint`, a hook only calls it. Gate `e2e:847-docs`. |
| 848 | **DONE** | [The contract, and what a name is worth](848-the-contract-and-what-a-name-is-worth.md) | From the first unattended run (99.2 % coverage, 0.0 % named): a contract written once before the human leaves, named-ness as its own measure, a blocker that may veto but never decide. Validated on a second unattended run with the same prompt: named 0.0 % → 39.3 %. Gate `e2e:848-contract`. |
| 849 | **DONE** | [Harness rules](849-harness-rules.md) | Six `paths:`-globbed rules shipped from `assets/project-rules/`, synced by `agent_onboard`, never overwriting a hand edit. Measured afterwards: a glob fires on a native READ, an RE session reads through MCP tools, so the rules are advisory at best — gates refuse, the critic refutes. `smoke:project-rules`. |
| 858 | **DONE** | [The daemon serves the project you are looking at](858-the-daemon-serves-the-project-you-are-looking-at.md) | The CART picker was empty because the daemon had been started for another project and nothing compared the two. One project source in TRX64, `ping` names it, `project/set` switches in place after a requester in the workbench. Released in TRX64 0.8.0; `smoke:858`. |
| 859 | **DONE** | [The line as the VIC saw it](859-the-line-as-the-vic-saw-it.md) | vicspector's view with the emulation's real data: a per-cycle recorder compiled in only for a scratch replay from the latest ring anchor, checked against the picture on screen (`verified`). `vic/line_trace`, `runtime_vic_line_trace`. Released in TRX64 0.8.0; `vic_line_trace_gate`, `smoke:859`. |
| 860 | **DONE** | [The frozen frame as a view](860-the-frozen-frame-as-a-view.md) | A VIC-view toggle in the freeze: a translucent raster-line × cycle grid over the whole frame including the blanking cycles, display objects by union-find over cells sharing mode, screen, charset/bitmap and bank, sprites per appearance, and techniques as deterministic rules over the record. `vic/frame_map`, `runtime_vic_frame_map`. Released in TRX64 0.8.0; `smoke:860`. |
| 861 | **DONE** | [The cost of a change](861-the-cost-of-a-change.md) | What a change touches, what the code costs, and what it costs on the machine. A graph walk ordered by depth with UNKNOWN as a class of its own; a cycle table that lives twice and is gated against itself, exact block costs, resolved loop bounds, liveness, and symbolic equivalence over two straight-line versions including the ORDER of every I/O access; and an evaluation of the trace the runtime already writes — Δclock is the cycles an instruction took, the table's answer has its page crossing and its branch resolved from the trace itself, an interrupt dispatch's 7 cycles belong to the entry, and what is left is what the VIC stole. TRX64 needed no change; two gaps in C64RE's own graph did (the exits no edge can carry, and a pointer table's xrefs the resolver dropped). Proved against the machine: 244 opcodes, 137 591 cycles measured against 137 591 from the table, 1075 stolen against 859's 1075 for the same frame, a store on line 100 cycle 13 in both records. §7.8 is the exception and is recorded as one: the drive lane is a deduplicated PC sample with no opcode, so the evaluation refuses it and names the smallest TRX64 change — one row per retired drive instruction carrying its opcode — rather than pricing a sample. `check:cycle-table`, `e2e:861-static`, `e2e:861-impact` (CI), `smoke:861` (local). |
| 862 | **DONE** | [Finding optimisation candidates](862-finding-optimisation-candidates.md) | The other half of 861: not what a change costs, but which changes are worth making. Eleven rules in one table — a `jsr`+`rts` that could be a `jmp`, a load nothing needs, a `clc` where the carry already is what it would set, a `jmp` to a `jmp`, a table read that pays for crossing a page, a variable that belongs in zero page, a loop that could count down, and two that rewrite into stable undocumented opcodes. Each rule is a heuristic and may be wrong; none of them is trusted. A straight-line rewrite is decided by symbolic equivalence over the registers, the stack, every memory cell and the flags that are live there, with every I/O access in order; a rewrite that is not straight-line is decided by a control-flow identity whose one obligation is read off the callee's own bytes; a structural rule answers with an exact count and says it is a count. A proposal that comes back NOT EQUIVALENT has proposed nothing, and the counter per rule makes a rule that is often wrong visible. Code whose purpose IS its timing is refused and listed with the reason, and nothing is ever applied — the scan opens the graph read-only and leaves an empty write-ahead log behind it. Two of the spec's own claims did not survive contact: the tail call saves 9 cycles and not 6, and the `sax` rule it names cannot exist, because the documented sequence it would replace writes a scratch cell that `sax` does not. `e2e:862-rules`, `e2e:862-rank` (CI), `smoke:862` (local) — 13 656 cycles of page crossing measured, 13 656 reported. TRX64 needed no change. |
| 863 | **DONE** | NTSC (`../../../TRX64/docs/_archive/863-ntsc.md`) | A C64 model is a row of TRX64's `models.toml` (VICE's model table); `c64-pal`, `c64-ntsc` and `c64-paln` run, a row naming a building block that does not exist is refused by name. The NTSC cycle tables are VICE's, ported 1:1; PAL stayed bit-identical and the palette stays Colodore. **A model change is a transplant at the frame boundary, never a power cycle** — the program keeps its state and the standard it detected at boot. C64RE: the Live-tab selector, `model` on `runtime_session_start`/`runtime_sandbox_run`/`runtime_scene_reel`, `knowledge/project.json → machine.model` passed as `--model` by the one spawn resolver, no `PAL_CYCLES_PER_FRAME` left (every frame count reads the machine), scenarios carry and enforce their recorded model, the VIC view takes its geometry from the recorder header. `e2e:863-model`, `smoke:863`. Merged 2026-09-20. |
| 774 | **WON'T-DO** | [Capability Cut → `trx64-static`](774-capability-cut-static-migration.md) | Closed 2026-09-19. Step 1 shipped 2026-07-02 and stays: the daemon's monitor and `trx64cli disasm` share one decoder crate, held to C64RE's TS decoder by a 512-case golden suite. Step 2 was dropped 2026-08-11 (drive and workbench read GCR by opposite rules). Step 3, the analyzers to Rust, is WON'T-DO: the owner decided TRX64 is a runtime — for `trx64cli`, C64RE and UE2 — and none of those needs a static classifier. Static analysis is C64RE's, permanently and in TS. |
| 740 | **DONE** | [Project Wiki + Knowledge Retrieval](740-semantic-search-vector-index.md) | Closed 2026-09-19. 740.1 (2026-05-31): `project_search` / `project_find_related` / `project_reindex_search` / `project_wiki_lint` over a deterministic, explainable index — no embeddings, no vector DB. 740.2 (`project_wiki_update`) answered by 847 D6 and never built: documents declare themselves and `wiki_index` derives the index. 740.3 built: the graph's human layer is found as itself (routines with their extent, labels incl. user labels, model boundaries at their real range, declared documents with their covers) instead of as generic entities; documents at any depth through 847's scanner; a render ranks as a copy and a drifted one below the live record, its `why` carrying the critic's own stale-render comparison (`src/docs/render-drift.ts`, one function for both); the cache fingerprints what it read and rebuilds on the read path, saying so in one line; one listing per disassembly (`.asm` > `.tas` > `.tass`). Measured before settling D4: full rebuild 0.6–1.1 s on Ultima VI, 1.0–1.5 s on Wasteland_EF; the per-answer check ~80 ms / ~17 ms — no split. Gate `smoke:740-graph` 52/52 (hermetic, in CI); `smoke-740-project-search` 30/30 against Wasteland_EF, which had PENDED since the 822.2 cut-over because it probed for `findings.json`. |

## Historical

The remaining ~150 files here predate this register. They are kept for archaeology —
searchable, referenced from commit messages and code comments — but carry no per-file
decision record, and reading one is not evidence that anything in it still holds.
Check `../README.md` and `../../DOCTRINE.md` first; if a rule is not in either, it does
not bind.