# Bugs

Human + LLM E2E bug log for the C64RE MCP/UI product flow.

- Copy `TEMPLATE.md` → `BUG-<NNN>-<slug>.md`, fill it in.
- One file per bug. Keep evidence verbatim (exact error / wrong output).
- `Status` lives in the file header (open / investigating / fixed / wontfix / duplicate).
- On fix, fill the **Resolution** section and name the gate proving it.

| ID | Title | Area | Severity | Status |
|----|-------|------|----------|--------|
| BUG-001 | `/v3.html` opens project dashboard instead of C64 Runtime Workbench | ui-v3 | blocker | fixed (597ad85) |
| BUG-002 | v1 legacy UI is still served as a separate product entry | workspace-ui | high | fixed (ec0a1e1) |
| BUG-003 | `extract_disk` manifest import fails on empty CBM directory filename | mcp-tool | high | fixed (17bad20) |
| BUG-004 | `build_all_views` crashes on empty manifest item title/name | knowledge | high | fixed (17bad20) |
| BUG-005 | Agent/audit recommends tools that are not exposed in MCP surface | mcp-tool | blocker | fixed (Spec 730 orchestrator + machine-readable next-step) |
| BUG-006 | `agent_onboard` reports filesystem/artifact store “in sync” for empty unverified workspace | mcp-tool | medium | fixed (ba181dc) |
| BUG-007 | BASIC PRGs at `$0801` are treated as 6502 code instead of BASIC programs | analysis | low | closed → backlog (Spec 731) |
| BUG-008 | Disk tab selection jumps back to first disk | ui-v3 | high | fixed |
| BUG-009 | Disk file list scrolls the whole page instead of the list panel | ui-v3 | medium | fixed |
| BUG-010 | Workspace UI Live tab does not start/connect Headless Runtime backend | workspace-ui | blocker | fixed (700b398) |
| BUG-011 | Analysis tabs render raw JSON instead of usable UI views | ui-v3 | high | fixed (4c4fdc7 — real viz) |
| BUG-012 | Media tabs render raw JSON instead of usable UI views | ui-v3 | high | fixed (4c4fdc7 — real viz) |
| BUG-013 | Live Drive insert menu mixes project media with C64RE dev samples | ui-v3 | medium | fixed |
| BUG-014 | Migrated workspace views lost shared Inspector and original layout | ui-v3 | high | fixed (bcbd770) |
| BUG-015 | Project init leaves media unsorted instead of placing it under typed `input/` folders | mcp-tool | medium | fixed |
| BUG-016 | Live C64 frame scales too large and pushes Monitor out of view | ui-v3 | medium | fixed |
| BUG-017 | Disk geometry lacks track/sector navigation for occupied non-directory data | ui-v3 | medium | fixed|
| BUG-018 | Product UI lacks visible runtime connection/session status for LLM-human coordination | ui-v3 | medium | fixed |
| BUG-019 | UI shows stale ASM version instead of latest/best artifact version | workspace-ui / knowledge | high | fixed (A: 8d4faf3d; B: Spec 730 §7 artifact version store) |
| BUG-020 | Header hero contains dashboard metrics and filters | workspace-ui | medium | fixed (user-confirmed 2026-06-04 — lean header, metrics/filters moved to Dashboard) |
| BUG-021 | Spec 741 fixture: relocated loader is demoted to unknown instead of rendered as runtime code | analysis | high | fixed (Spec 741 A–D; smoke-741 50/50) |
| BUG-022 | `.gitignore` `analysis/` rule also ignores `pipeline/src/analysis/` source | build / repo-hygiene | medium | fixed |
| BUG-023 | Custom true-drive (drive-side GCR) writes not persisted to host .d64/.g64 file (Wasteland Utils Copy + Scramble HighScore) | runtime | high | fixed (VICE-faithful write-through: fsimage_*_write_half_track → hostFlush writes the host file at the writeback commit; `smoke:023-write-through` 7/7 re-reads file+mtime, no unmount. Doctrine: VICE fwrite must map to host writes; tests re-read FS) |
| BUG-023-cart | Writable cartridge (EasyFlash) flash writes not persisted to host .crt | runtime | medium | fixed (EasyFlash `getCrtImage` re-pack + `persistCartridgeToFile` writes host .crt on eject; `smoke:023-cart` 7/7. Spec 742) |
| BUG-024 | Code-derived disk loads (no CBM dir / no on-disk LUT) can't be registered as rich payloads — `save_entity` makes thin records | mcp-tool | medium | fixed (`register_payload` promoted to default + `source_prg_path` + ASM stem-match; `save_entity` redirects payloads; gate `e2e:024` 15/15) |
| BUG-025 | Inspector/Frozen overlay poisons Pause/Run resume with alarm-dispatch guard | runtime / live-ui / inspector | high | fixed (Spec 743 — maincpu CLOCK made monotonic, no clkguard; removed u32/0xffffffff on absolute clk across CPU+alarm+interrupt+VIC+CIA/ciat/TOD; `probe:743` 42/42 + cia-suite 16/16 + 7-game PNGs read) |
| BUG-026 | Host ESC is mapped to RUN/STOP instead of C64 ESC | runtime / live-ui / keyboard | medium | fixed (ESC→C64 ← / ^→CTRL / TAB→RUN/STOP in keymap.ts + Live.tsx; `smoke:026` 11/11) |
| BUG-027 | Cannot reach in-game from a boot: trace worker module-not-found, no workable in-disk "insert side N" swap, headless free-runs ~100% CPU when idle | runtime | high | fixed (Blocker 1 trace-worker FIXED 744.2 `e2e:744-2`; Blocker 3 idle-CPU FIXED — MCP runtime is idle-bounded `probe:744-idle` 8/8 + `runtime_session_close` `probe:744-3`; shared MCP+UI authority FIXED via Runtime Daemon 744.4c (`e2e:744-4c` 9/9 — MCP+UI both clients of one stable runtime, reconnect/reload do NOT reset; 744.4a in-process `smoke:744-4` 16/16, 744.4b co-host RETIRED); Blocker 2 disk-swap FIXED 2026-06-05 (Spec 744 §7.2): `runtime_swap_disk_and_continue` shipped — eject→run→insert→run→RETURN→run as ONE call (atomic swap gives the drive no cycles to sense the change; the UI works because it's this hardware sequence). Shared `swapDiskAndContinue` + daemon op + MCP tool, `e2e:bug027-swap` 8/8 + probe:single-path 25/25. Game-advance on a real multi-disk title is live-verified. The old `diskunit_clk` timing trace is moot — running the C64 between eject/insert advances the drive clock) |
| BUG-028 | Runtime input/drive tools not daemon-routed: `runtime_type` (et al.) fail "No integrated session" on the shared daemon session while `runtime_session_status`/`runtime_render_screen` work — can read shared session, can't drive it | mcp-tool / runtime | high | fixed |
| BUG-029 | Trace-store reads (`runtime_swimlane_slice`/`trace_store_*`) fail "Could not set lock" while the daemon process is live (DuckDB cross-process lock); store reads fine once the daemon exits. Fix = route trace reads through the daemon (746.5). | trace-store / runtime daemon | medium | fixed |
| BUG-030 | Trace aborts on backpressure during the fastloader firehose (256-chunk ceiling → whole trace lost, 819 B stub) AND wedges: post-abort `finalize` keeps re-aborting, `trace_start` says "already active", no working stop → needs daemon restart. Blocks tracing through disk loads (map-load/writeback/save). | runtime / trace | high | fixed (`session/run` segments+drains so the firehose feeds the worker; `stop()` always-clean so an abort never wedges — `e2e:bug030` 9/9) |
| BUG-031 | Disk-layout view ignores registered-payload `medium_spans` — shows only CBM manifest + BAM, so code-derived raw disk regions (no dir entry) never appear on the disk geometry (Wasteland disk looks empty save prodos/2.0). Disk-view analogue of BUG-024. | knowledge / ui-v3 | medium | fixed (Spec 750.1: builder overlay-pass + grouping `e2e:bug031` 14/14; DiskPanel renders the grouped `origin=custom` entries + `custom`/`unscoped` badges off the live snapshot — UI render user-confirmed 2026-06-05; per-image scoping via `mediumRef`, no-ref = shown-on-all + badged) |
| BUG-032 | No steering/persona makes the agent record findings + reconcile open questions after each action → answered Qs stay `open`, findings unlinked. Plus: heuristic Qs need an `autogen` tag + per-pipeline-step lifecycle (revisit the artifact's autogen Qs on re-process → close/update/delete via the existing `auto_resolvable` field) so the Questions screen stays meaningful instead of accumulating stale noise. | mcp-tool / agent-steering | medium | partial (Spec 748.1 ships the steering VEHICLE — `project_steering_set` → `knowledge/steering.md` injected at the top of `agent_onboard` every session, `e2e:748` 6/6; ENFORCEMENT via state-derived `agent_next_step` + heuristic-Q autogen lifecycle = Spec 748.2, open) |
| BUG-033 | `missing`/`stale` artifact-version flag is sticky — not cleared when the file reappears on disk (regenerated byte-identical rebuild); and auto-current picked a `related` `.sym` (rank 50) over the primary `.asm` (rank 100, flagged missing). | artifact version store | low | fixed-main (`reconcileArtifactVersionGroups` clears the flag by FS presence + auto-current prefers a primary over a `related` companion — `e2e:bug033` 7/7). Secondary (file-space `routines[]` renames the auto-label too) FIXED 2026-06-05 — `buildAnnotationsIndex` seeds `labelsByAddress` from a routine `name` (sanitised via `toLabelIdent`, explicit labels + collisions keep the auto-label), `smoke:bug033-label` 7/7, no regression. |
| BUG-034 | Effective-segments overlay (Spec 055) not reused: annotation segment reclassifications (e.g. a mis-disassembled data region → `data`) show ONLY in `disasm_prg`, not in `inspect_address_range` / memory-map / graphics / annotated-listing views; `resolve-pc` re-implements the overlay incorrectly (append-not-overlay). Render-private algorithm duplicated 3×. | analysis / knowledge views | medium | fixed (Spec 751 option D — one shared server-side `effective-segments` module routed through all real consumers + `resolve-pc` overlay fix + `service.ts` collapse + memory-map view-time overlay w/ `reclassifiedByAnnotation` flag + inspect-range `[reclassified by annotation]` hint; non-destructive, no `_analysis.json` write-back; `e2e:751` 27/27. The 6th surface `view-builders.ts:251` was a false positive.) |
| BUG-035 | `c64retrace` format bumped to v2 (`binary-format.ts:29`) with no read back-compat, AND the live daemon's writer still stamps v1 (header `C64RETR1` + uint16 v1 @off8) → reader rejects (`:126-127`) → every fresh capture fails background indexing (no `.duckdb`), so `trace_memory_map`/`trace_store_*` are dead on new traces; 7GB+ historical v1 captures orphaned. Re-capture does NOT help (writer still v1). | runtime / binary trace log versioning | high | fixed (read-compat: `decodeFileHeader` accepts v1..CURRENT, `decodeEvent`/`decodeEventStream` take the header version → v1 mem-access decodes at 14-byte width; writer skew is non-fatal; v1 corpus re-indexes in place. `e2e:753` 34/34. + `trace_memory_map`/sidecar now `ensureTraceIndex` so an orphaned `.c64retrace` indexes on first read.) |
| BUG-036 | Monitor has no coherent pause/resume — live `g` is a bounded burst (`ctrl.pause()` + 1-frame `runFor` with no bp) that ends HALTED, not the continuous free-run the Run button uses (`ctrl.run()`/`continue()`); no explicit `pause` verb. The run-state machine already exists; `g` bypasses it. | ui-v3 / runtime | high | fixed (Spec 754 §3.1: `g`→`ctrl.continue()`, `g addr`=set-PC+continue, `x`=resume; bounded-burst dropped; `until`=synchronous run-to-landing; Pause-button=halt. `e2e:754` 22/22 — g→running, resume→bp→pause, until lands halted) |
| BUG-037 | Two divergent monitor command processors: live `monitor/exec` VICE-parser (`v3-ws-server.ts:1633`) vs `Monitor.tsx` + `monitor-cmd-parser.ts` → `runtime/call`. Disagreeing command sets (dump/undump + flow-focus only in one; `g` semantics differ). | ui-v3 | medium | fixed (Spec 754 §3.2: extracted the one canonical processor `monitor-shell.ts` = `runMonitorCommand`; `monitor/exec` is now a thin adapter; retired the dead `Monitor.tsx`+`monitor-cmd-parser.ts`+`MonitorCmdLine.tsx` triangle. `e2e:754` Part C) |
| BUG-049 | A client's focus loss cleared another client's joystick. `runtime_joystick` drove the title menu of a game and did nothing in play, while the UI's WASD→port-2 path worked in the same session — so it read like the MCP path was broken. It is not: measured against a sandbox daemon with C64RE's own client and the tool's exact payload, `$DC00` goes `$ff → $f7 → $ee`. What killed it was `session/release_keys` clearing **both** joysticks globally (the TS focus-loss policy), which the browser sends on `blur` **and** on every teardown of an input effect keyed `[sessionId, runState, joyMode]` — plus `joystick_set` writing the whole port, so any UI keyup overwrites. The UI never noticed because it re-sends its full stick on every key event; a tool that sets once has nothing that re-asserts. The reporter's `$DC00 = $7F` readback proved nothing either: `read_full` returned the bare CIA latch, so the peek could never show a key or a stick. | runtime / shared input state + monitor peek | high | fixed (`release_keys` releases KEYS only — clearing a stick is `joystick_clear` with a `port`; `Live.tsx` clears only the port it drives; `$DC00`/`$DC01` peeks compose the pins via one shared `keyboard::cia1_pa_pins`/`cia1_pb_pins`, which had been written twice and present in only one of the two read paths. Gate crosses both ports. Same family as BUG-048) |
| BUG-061 | Under turbo a PAL C64 decided it was NTSC and ran its jiffy clock 3.3% slow. The KERNAL decides by racing the CPU against the raster at `$FF5E` ~1.5 s after reset; a turbo CPU wins it from 16 MHz up. The Ultimate holds its C64 at 1 MHz for 2.06 s after every reset (measured, reset-anchored, same at 16 and 64 MHz), so on hardware the race always runs at 1 MHz. A first fix (v0.8.6) assumed the firmware applies the speed after boot; it strobes 445 cycles after reset release, so that fix did nothing on the real firmware path. | runtime | high | fixed (`vic.u64_reset_hold`, 2.06 s of PHI2 armed by every reset; gate `u64_boot_speed_gate.rs` 5 tests, red without it; only `booted@64` re-recorded) |
| BUG-060 | Thirteen defects from the second unattended Neuromancer run — 230 listings, all 230 rebuilt byte-identical, cartography 100 % against the first run's 89.4 % — plus two a peer session reported against the graph the same day. **The graph one is the worst:** `seedControlFlowForArtifact()` had exactly one caller, the analysis-report import, so a project migrated by `agent_onboard` got the human and segment layers and **no control-flow edges at all**; in the reported project one owner of about 25 carried 819 rows, and `graph_edges` answered nothing for a resolver whose caller stands in plain sight in the listing. `orphaned` was computed per id, so a human `label` whose generated twin at the same address is a `segment` read orphaned though the address was covered — which is what made that project's session conclude the graph had lost its prose layer. `PRAGMA journal_mode = WAL` needs an EXCLUSIVE lock and **SQLite does not run the busy handler for it**, so every writer's `timeout` bought nothing and the pragma ran on every open. `extract_disk`'s chain walk `break`ed out silently on an unreadable or revisited sector and published the reachable prefix as a measured extent — the Pawn 168/1329 shape, with the one guard that exists for it blind because the blob stops at the same broken link. A `code` segment over bytes ending mid-instruction emitted `.byte` with no value, which assembles to nothing, so the rebuild came out a byte short. Two annotation segments on one start failed in one buried line beside `rebuild verified byte-identical`. `project_inventory_sync` recommended registering 2732 per-sector dumps and **S12 coverage fell 22.2 % → 7.6 % without a byte becoming less understood**, with no inverse. Plus: a payload filed under the CBM name while its listing keys on the file stem, the extractor's own PRG header counted as a missing sector, no bulk door on `disasm`/`analyze`, a LUT descriptor that could only describe a table on the medium, a title cap that surfaced as a schema dump, and a model boundary of containers reporting empty. [BUG-060](BUG-060-thirteen-defects-from-the-second-neuromancer-run.md) | mcp-tool | high | fixed (five parallel branches, merged the same day, every defect proved red first. Two reported shapes did not reproduce literally and the agents reported what they found instead of fitting the claim. New gates `e2e:disk-chain-spans` 30/0, `e2e:822-cutover-seed` 21/0, `e2e:bug060-code-tail` 24/0, `e2e:bug060-dup-segment` 11/0, `e2e:060-bulk` 38/0, `e2e:060-slot-title` 21/0; extended `e2e:inventory-truth` 86/0, `e2e:subject-identity` 56/0, `e2e:750-lut` 125/0, `e2e:818` 31/0, `e2e:store-concurrency` 35/0. **Three gates that ran nowhere are wired**, and `e2e:823`, once it ran, immediately caught a real drift it exists for: Spec 867 gave `graph_find` its claimants and the CLI never got them, two surfaces one spec calls one document at 25 lines against 38) |
| BUG-059 | Nine defects a third autonomous run wrote down, two of which are not bugs in a door but in a MEASURE. S12's coverage counted bytes inside a NAMED range, so the run raised it by painting `unknown` segments called `unnamed_XXXX` over ranges it had already named as routines — the loader among them — and then reverted all 47; and S6 was gated on a regex finding a digit immediately before the word "runtime" in S5's prose, so "Two permanently resident images and twelve swappable windows" was unreadable, "4 resident runtimes and 14 swappable windows" was too, and S6-S9 stayed permanently n/a. The other seven: a `routines[]` entry with no `comment` crashed the renderer after the loader reported `skipped 0`; a note fired ~35 times naming a CLI flag an MCP caller cannot pass (no positional caller exists — `entry_points` was the only way in, and it was never checked); a boundary in space `drv` over $0300-$07FF contained nothing because `platform: "c1541"` was used and never recorded; `project_inventory_sync` returned 146 728 characters; a required parameter lost after a multi-paragraph answer came back as a schema dump (the transport takes 4 MiB — the call was cut short as the caller wrote it); two G64 doors called one block `gcr_error` and `checksum_error`; and reading four sides one track per call cost 140 round-trips. [BUG-059](BUG-059-nine-defects-from-a-third-autonomous-run.md) | mcp-tool | high | fixed (S12 counts what is CLAIMED — a range classified `unknown` never counts, any other classification does, an unclassified range counts only under a human name, and the two excluded buckets are printed so the reader can see what would move it; S5's count is a FIELD and the prose fallback reads both reported sentences; `comment` is optional and `name` is refused by name and address; `entry_points` is read by the one address rule and a `.json` is sent to `analysis_json`; a named platform is recorded on both rails the graph reads and an empty boundary says which space the bytes ARE in; the sync's detail goes to a report file the answer names, an empty declared glob is diagnosed against what is in that directory, and unregistered tool output is reported; one GCR vocabulary with each door stating the condition it tested; and `tracks` / `all_tracks`. `e2e:844-slots` also joined CI, which is how both measures went unobserved) |
| BUG-058 | The knowledge store had two writers in two processes — the MCP server's `saveArtifact` and `registerCliArtifact` in the pipeline child, from nine call sites. That is why the store needed a cross-process lock at all (BUG-056) and why a legacy entity writer could sit on the far side of that boundary for months (BUG-054). The survey nobody had done: of the thirteen pipeline verbs an MCP door can reach, ONE output was registered by the child alone — `propose_annotations`'s draft — and three doors (`reconstruct_lut`, `export_menu`, `disasm_menu`) registered nothing at either end, so their manifests, payload binaries and listings landed in the project and the project did not know. [BUG-058](BUG-058-two-writers-on-one-knowledge-store.md) | mcp-tool | high | fixed (the missing registrations are done from the parent through `tryRegisterKnowledgeArtifacts`, with the NOT-REGISTERED banner leading the answer; the MCP spawn path passes `--no-register` through one named helper so a new call site cannot acquire a second writer, while a direct `dist/pipeline/cli.cjs` run — which has no parent — keeps registering what it writes; and the child signs its writes from inside, because the store cannot be asked whether the child wrote it. `e2e:one-store-writer` 63/63) |
| BUG-057 | The three BUG-056 left open, each parked because the fix moves data an existing project already holds: `subjectIdForArtifact` was the basename with the directory thrown away, so three disks' `pl0_disasm.asm` were ONE subject with three tying versions and `get_current_artifact("pl0")` could hand back another disk's code; `isVersionedSourceArtifact` did not know `.tas`, the suffix the renderer has written since 2026-09-06, so every modern 64tass listing joined no version group at all; and `basic_tokenize` was the only MCP door that left registering its output to the pipeline child. [BUG-057](BUG-057-three-defects-that-move-a-projects-data.md) | mcp-tool | high | fixed (a subject is the lineage a file belongs to — a declared `derivedFrom`, else the directory plus the stem — and a bare filename that names several subjects is answered with the list; `.tas` joined the version model with a named dialect rule so the KickAssembler listing the 64tass one was converted from keeps the current; `basic_tokenize` registers from the parent and names its knowledge run; and a project written under the old subject key is re-keyed on first open with its pins, stale marks, group ids and orphaned members intact. `e2e:subject-identity` 49/49) |
| BUG-056 | Ten more defects from the run that produced BUG-055, fixed in three parallel branches: the knowledge store could not take two writers (one fixed `.tmp` name, six ENOENT, and one registration lost in silence under a success message); `extract_disk` counted three files that produced nothing among the done; one sync raised 220 open questions because the subject id is basename-only; the leftovers rotated because a content-hash match MOVED an existing row instead of making a second one; `agent_record_step` and `project_inventory_sync` disagreed 840 against 831 because the project's declaration reached only one of them; `disk_sector_allocation` kept its map to itself and let scratched entries claim the directory; `sandbox_6502_run` called its own zero-page stores residue; the depacker doors had no `project_dir`; a table ending at row zero said nothing; and the contract could not check a document about a medium. [BUG-056](BUG-056-ten-more-defects-from-the-same-run.md) | fixed | 2026-09-20 |
| BUG-055 | Six defects in the disassembly doors, from the run after BUG-054: BUG-054's own address rule came back in a new door because it had been fixed by being WRITTEN DOWN twice rather than made one function (two `ADDRESS_RULE` constants, four inline `parseInt(x,16)` sites, one of them never stripping a `$`); `disasm_raw` pointed at a 640-byte window of a 63 KB image picked up the sibling whole-image analysis and rendered 929 instructions and 4082 data lines over 640 bytes, with no way to say "no analysis"; `disasm_prg` pushed `analysis_json` onto the positional tail BEHIND the entry-point list, which is only filled when there ARE entry points, so a call with an analysis and no entry points put the path where addresses were expected, got NaN and fell back to the stem-matched sidecar; `disasm_raw` applied annotations and never imported them, so drive code — whose only door it is — had no human names in the graph at all; ~500 seeded owner names went into every analysis and disassembly result and every listing header, on each of 292 payloads; and a packed stream's `jump distance is too far` said nothing about the remedy. | mcp-tool | high | fixed (one `src/shared/address-rule.ts` compiled into both halves with `check:address-rule` refusing a third copy; a raw window inherits no analysis and `no_analysis` refuses one; `--analysis` is a named flag and a missing path is a refusal; `disasm_raw` imports the path the RENDERER reports; the owner list is a count plus the closest names, `C64RE_GRAPH_SEED_OWNERS=full` for all; the rebuild verdict names the packed-stream diagnosis and both ways on. `check:address-rule` 9/9 + `e2e:disasm-family` 33/33) |
| BUG-054 | Thirteen tooling defects one autonomous RE run wrote down as it hit them, ordered by what they cost: a payload door that could not see payloads `extract_disk` created; `relocations` reading hex differently from `entry_points` in the same call, and a zero-page entry throwing a node stack; 232 chain-coverage warnings about payloads that are simply packed; `declare_lut_descriptor` silently demanding an absolute `medium_path`; `doc_register` refusing one field at a time and rejecting quoted `covers` items, while the rule that states the contract was structurally undeliverable; an L2 auto-chain that disassembled 245 files and verified none; a written-run list truncated with one 2000-byte run behind the ellipsis; a coverage percentage no work could move, over a slot report contradicting itself; a slot answer becoming the finding's title; a blocking verdict dropping the text that settles it; a skipped count hard-capped at 10 for 616 files, with no way to declare a directory intentional; an audit recommending a tool the session cannot see; and `exomizer_sfx` at 0.93 for "some code ran and wrote memory" on a $B3-escape RLE. | mcp-tool | high | fixed (thirteen causes, one round — see the entry; gate `npm run e2e:tooling-defects`, 87 checks) |
| BUG-053 | F9/F10/F12 did nothing in the C64RE UI while F11 paused and resumed. Not the keys and not the wire: `transport/key` was called, answered `handled:true`, and the machine's clock actually advanced — the canvas kept showing the frame from the moment of the pause. The VIC frame push lived inside the stream pump's `if running` branch, so a machine the rewind transport moved while PAUSED was never presented (a monitor `z`/`n` on a paused machine fell in the same hole), and `play back` — which sets a direction and leaves the stepping to the pump — was ticked only in that same branch, so F10 took no step in the one state you press it in. The daemon's refusals (`handled:false` + reason, "no anchors yet — the ring fills while the machine RUNS") were discarded by the Live tab, so a correct answer reached nobody. | ui-v3 / runtime | medium | fixed (paused branch ticks the transport and presents when the machine MOVED — `paused_present_due`; `Live.tsx` prints the daemon's own words. Gates `a_paused_machine_that_moved_owes_a_frame` + `play_back_steps_a_paused_machine`) |
| BUG-052 | `runtime_loader_lens` on a finalized 2.1 GB / 118 M-event `.c64retrace` returned `Connection closed` and took every `mcp__c64-re__*` tool with it until `/mcp`. Two unbounded allocations: `readFileSync` pinned the whole log, then `decodeEventStream` materialized one JS object PER EVENT. The readers run in the MCP server process, so the OOM killed the transport — the daemon, being a separate process, never noticed. `validate_extraction` read the same file three times. The indexer next door already streams and says so in a comment; the loader-lens readers never got the message. | trace | high | fixed (`capture-stream.ts` = the one streaming reader, extracted from the indexer's own loop and now shared by both; the three builders became folds; one pass for both lanes; a readable error past 250k runs; `runtime_loader_lens` on the BUG-039 job registry for the stall half. Gate `npm run smoke:bug052`, 16 checks incl. a heap bound) |
| BUG-051 | A Spindle-loader demo booted, streamed and ran — right mode, right `$D018` — and every pixel was garbage: the VIC fetched from bank 3 while the demo's screen and charset sat in bank 0. The bank was derived as `(PRA & DDRA & 3) ^ 3`, which reads a pin configured as an INPUT as 0. VICE `core/ciacore.c:810` puts `PRA \| ~DDRA` on the port — an input pin floats high on its pull-up and contributes **1** — and `c64/c64cia2.c:150-151` takes `~byte & 3`. Three copies of the wrong formula plus `vic_inspect.rs`, which used the PRA latch alone and never read the DDR. The KERNAL leaves `DDRA = $3F`, both bits driven, where the two formulas agree — which is why the seven-game gate stayed green and only a loader that drives `$DD00` itself (`DDRA = $3C`) exposed it. | runtime | high | fixed (all four sites take the port value; gate `vic_bank_ddr_gate.rs` covers driven, floating and mixed, verified RED against the old formula) |
| BUG-050 | The machine free-runs between two calls, so no boot recipe is repeatable. A session is left RUNNING after the calls that set it up: measured **1,985,352 cycles in 2000 ms** of wall clock (~993k cyc/s). Every gap between two calls — a round trip, an LLM thinking — is therefore part of the machine's state, and no two attempts start their first command from the same place. Reproduced with a control on two disks: uncontrolled, one recipe with only the caller's latency varied gave **5 of 5 distinct screens**; with one `debug/pause` after the mount and the boot expressed in cycles, **1 of 5** — identical to the cycle. Sharper form: `session/power op:"on"` sets `running`, so 2 of 10 fresh machines were already a whole frame (19657 cycles, `$FCE2`→`$FD77`) into the KERNAL reset before `debug/pause` landed. There is no way over the API to get a machine stopped at a known cycle. The core itself is deterministic — Spec 812's reel hashes identically three times over 100M+ cycles through a fastloader. Same family as BUG-048 and BUG-049: shared machine state each client assumes it alone moves. | runtime / session lifecycle | high | open |
| BUG-048 | Two run flags for one fact, so pause did not stop the machine. `play_intent` was read by `session/tick` + the transport; `session.running` by `debug/run`/`debug/continue` + the `--stream` loop; each was set and cleared by a disjoint set of verbs. The WL editor starts with `debug/run` and pauses with F11 → `transport/toggle`, which cleared only `play_intent` — so the machine kept consuming cycles. `session/state` reported the **OR** of the two (`play_intent \|\| session.running`), which lied in both directions: "running" for a paused machine and "running" for a frozen one. The tell was already in the tick, which translated one flag into the other on entry because it could not trust what it read. This is Spec 808's own bug: 808 deleted the CLI's private `running: AtomicBool` for exactly this reason and kept a second copy inside the daemon, with a comment calling the split deliberate. | runtime / daemon run state | high | fixed (ONE flag — `session.running`; `play_intent` deleted, the tick's adopt block gone, `runState` read straight. Gate `pause_stops_a_machine_started_by_the_debugger` crosses the pairings: `debug/run` + `transport/toggle`, then `session/play` + `debug/pause`, asserting the clock stands still across four ticks. Reported by WL1 — with one correction: the sidecars run without `--stream`, so the cycles came from `run_until_break`, not an autonomous loop) |
| BUG-047 | A deleted source file stays registered and can win the `current` tie: the Disk view's "open best available source" served `block2_engine_0200_disasm.tass` as **Current (auto)** — with the store's own note "two sources tie on rank" — and the body was a bare `HTTP 404`. Both versions carry `rank=0`; the `.tass` has no file behind it. Not one stray record: **184 `.tass` paths registered, 2 on disk**, many pointing at `/tmp`. Probably the aftermath of a deliberate "tass out, KickAss only" decision, which is exactly the case handled worst. The demotion machinery is correct (`markArtifactVersionStatus(..,"missing")` re-picks a present candidate) but its only callers are manual verbs, and `computeArtifactVersionGroup` ranks without ever asking the filesystem. BUG-033 built the other half — clearing the flag when a file returns; nothing sets it when one leaves. | knowledge / artifact versions + ui | medium | open (presence must disqualify, not tie-break; reconcile must mark absent versions; a missing version must say so instead of 404) |
| BUG-046 | The keyboard matrix could only be scanned ONE way. `keyboard.rs` had `read_rows_for_pa` and no mirror, and the `$DC00` read never consulted the keyboard — its own comment naming the assumption: "the keyboard back-scan (= 0xff in the dominant case — KERNAL never drives PB columns)". The title's IRQ scans BOTH ways (`DDRB=$ff, PRB=$00, LDA $dc00`) and returns the moment it reads `$ff`, so no key ever reached the game — it looked like a broken disk swap. Found by measurement, not reasoning: observers showed the handler's `RTI` hit 477× while `INC $3c`, the queue push, hit **0**. | runtime / CIA1 + keyboard | high | fixed (`read_columns_for_pb` mirrors `read_rows_for_pa` over one shared key walk; `$DC00` composes it like VICE `read_ciapa` — `pb_out=(PRB\|~DDRB)&joy1`, result `(val&(PRA\|~DDRA))&joy2`. Three gates incl. the game's register sequence verbatim. Ghost keys still unmodelled in BOTH directions, deliberately and stated — one-sided ghosting would make the two disagree) |
| BUG-045 | The D64 GCR encoder truncated EVERY image to 35 tracks: `D64_TRACKS` was a constant, and `from_d64` 0x55-filled anything above it (`d64_linear_sector` and the write-back carried the same cap). The title's disks are 40-track images — 196 608 bytes = 768 blocks — whose tracks 36-40 are 99.7 % full of game data, so **85 blocks per disk** became an unformatted track and the loader stalled on the disk it had just been given. Detection was innocent and that hid it: `D64_SIZES` listed 196 608, so the mount reported success. VICE derives the count from the file length (`disk_image_check_for_d64`, 35→42, +17 blocks per track) and compares against `image->tracks`, never a constant. | runtime / gcr encoder | high | fixed (`d64_tracks_for_len` ports the VICE walk; `from_d64`/`write_dxx_half_track` derive per image; `d64_linear_sector` takes the count; `D64_SIZES` replaced by the walk so 36-42 tracks are recognised too. Five gates incl. write-back to T40 and "a 35-track image still leaves T36 empty". Verified on the real sideA.d64: 85 sectors on tracks 36-40 round-trip byte-exact) |
| BUG-044 | Cockpit input stalled intermittently — a command visible on the line did nothing until the 3rd–8th Enter. Not lost, DELAYED: the TUI refreshed its panel every 50 ms via `engine.snapshot()`, an RPC taking the daemon's state lock, while Spec 808's cadence-1 default made the pump hold that lock 50×/s for a full checkpoint (plus auto-persist and the recorder feed, moved onto the same path by BUG-041). Ruled out first by measuring: the verb works one-shot, the log has no dedup, Up/Down are the only history path, and the `> {line}` echo is unconditional — so no echo meant Enter never reached its branch. | cockpit TUI / runtime | high | fixed (input queue drained first; the panel refreshes only when nothing is waiting — a stale display frame is invisible, a swallowed keystroke is not) |
| BUG-043 | Tab completion left the line ATTACHED to history (`hist_idx`), so a recalled + completed line was silently replaced by the history entry on the next arrow key: screen showed `/mount .../map_s1.d64`, `map_s2.d64` ran, three times. Char/Backspace/Delete each detach — two of them with the comment "editing a recalled line detaches it" — and `autocomplete` assigns `cp.input` directly without it. The log echo settles it: it prints the executed string, and it said s2. | cockpit TUI | high | fixed (autocomplete clears `hist_idx`; gated at the SOURCE, since this breaks when someone adds the NEXT editing verb and a behavioural test only covers today's paths) |
| BUG-042 | Observer `do log` stamps cyc/PC/A **per run-segment**, not per event: ~130 consecutive hits share one `cyc=24578787 pc=$093C a=$06`, then the next block shares another. Root cause verified — `main.rs:3051` refreshes the observer env once at segment start and `on_access_policy` reads `self.env.pc` for every access in that segment; `on_access(kind,addr,value)` carries no PC/clk at all, while `on_bus` (which the core already feeds pc+clk) is a no-op in the registry. **Second, worse consequence:** the same stale snapshot is what register CONDITIONS (`if a==$06`) evaluate against, so an observer can fire on the wrong events and miss the right ones — and that half looks like valid output. Reported by peer session WL1 during a BB2 depack. | runtime / observers | high | fixed (`AccessCtx{pc,clk,a,x,y,sp,p}` handed to `on_access`; `FullScBus.core_regs` raw pointer like the existing `core_pc`/`core_clk`; conditions and log fields read it, exec path unchanged. Gate `observer_stamps.rs` runs a REAL store loop over 200+ accesses and asserts consecutive cycles distinct+increasing and >8 distinct accumulators — both single-valued under the bug; a two-store test would have passed) |
| BUG-041 | `runtime_session_start` REQUIRES `disk_path`, which a shared attach never acts on (the daemon says so itself), and there is no cart/PRG/snapshot equivalent — so a cart-first workflow reads "start with a .g64 you do not need, then mount the CRT". Leftover from the era when a session meant a drive with a medium. Wants one optional `media_path` decided by CONTENT (`.c64re`/`.crt`/`.g64` by magic, `.d64` by size+BAM, `.prg` only as the last resort), with the owner's autostart rule: `$0801` + a valid BASIC line → `RUN`, else load only — which covers SYS-stub releases too, since the stub IS how they are meant to start. Detection belongs in the DAEMON, not the tool. | mcp-tool / runtime | medium | fixed (`detect_media_kind` = magic → size → prg-as-fallback; `media/open` is the one door and DELEGATES to the handler owning each type; MCP `media_path` optional with `disk_path` as deprecated alias, opened after the session exists; monitor `identify`. Gates cover magic-beats-extension both ways and the four autostart cases incl. a `10 SYS 2061` stub. OPEN: monitor `mount`/`eject` need the media handlers extracted to `&mut State` functions first) |
| BUG-040 | Verb surfaces diverge between the two front-ends: `/pause`, `/run`, `/mount`, `/warp` exist only in the TRX64 cockpit (the `/` prefix is a trx64-cli convention the daemon never sees), so the C64RE monitor answers `unknown command: /pause`. Worse, `pause` WITHOUT the slash exists there and means the Spec 808 transport pause — one word, two meanings, two surfaces. Same family as BUG-037, one layer up: not two parsers now, but two verb SETS. The daemon already has every capability behind them; only the verb is missing. | runtime / ui-v3 | high | partly fixed (`run`/`warp` are daemon verbs; `pause` now stops the machine AND the transport and prints the ring range; cockpit `/warp` delegates. Gate `machine_verbs_exist_in_the_daemon_and_pause_means_one_thing`. OPEN: `mount`/`eject` folded into BUG-041 where the media handler gets extracted anyway; and the C64RE UI must RENDER daemon status instead of composing its own, or the surfaces drift again through the output) |
| BUG-039 | Daemon per-session memory leaks: media-ingress pins before+after checkpoints forever (un-evictable in the 128 MiB ring; `runtime_swap_disk_and_continue` = 2 ops/swap accelerates it), `undump` clears mediaEvents without unpinning, `disposeMonitorShellState` never called, ws-server `monitorMemAddr`/`monitorDisasmAddr`/`inspectEvidence` not cleared on session/close. (Found by a leak-hunt while ruling memory out as the FPS-drop cause — FPS drop is environmental, not memory.) | runtime / checkpoint-ring / ws-server | medium | fixed (window pins to last 16 events + unpin roll-off; undump unpins before clear; close calls disposeMonitorShellState + deletes the ws maps. `e2e:checkpoint-pin-leak` 3/3 — 160 ops pinned 24→24; no regression single-path/709/707/027) |
| BUG-038 | Monitor `m`/`d` read raw `c64Bus.ram` → banking-blind: `m e000` shows RAM under KERNAL, `m d000` RAM under I/O, not what the CPU sees. (The help even suggests `d e000`.) | ui-v3 / runtime | medium | fixed (Spec 754 §3.3b/§3.4: side-effect-free `peek(addr,lens)` on the memory bus = VICE `mem_bank_peek`; `m`/`d` take a bank lens cpu\|ram\|rom\|io\|cart, default cpu = what the CPU sees; VIC/SID/CIA/cart got side-effect-free peeks. `e2e:754` Part A) |
